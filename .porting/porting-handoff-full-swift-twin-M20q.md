# Full Swift Twin Handoff — M20q

## Status

M20q is complete locally as the owner-thread Tuxie mother geo-eye route.
The mother environment carries the global timer; the bridge evaluates the
M20p callback with the post-behavior forward velocity and behavior identity,
then publishes the selected graph eye case in each generation-safe effect.
The moving chase branch therefore produces case 3 while stationary/blink
ticks preserve the authored cadence.

## Evidence

- Focused Swift 6/C owner-bridge fingerprint: `0xfbaf45212b54a77d`.
- Focused scripts:
  - `script/test_tuxies_mother_eyes.sh`
  - `script/test_tuxies_mother_eyes_object_bridge.sh`
  - `script/test_tuxies_mother_object_bridge.sh` (regression)
- Full matrix: `/tmp/sm64-modern-m20q-final-matrix.log`,
  `MATRIX_RESULT runs=172 failures=0`.
- Regenerated native Debug build: `/tmp/sm64-modern-m20q-build.log`,
  `** BUILD SUCCEEDED **`.
- `git diff --check` and zero unchecked-Sendable audit pass.

## Changed surface

- `SM64Modern/TuxiesMotherObjectBridge.swift` carries `globalTimer` and
  publishes `eyesSelectedCase` from the owner-thread mother effect.
- `script/test_tuxies_mother_object_bridge.sh` includes the geo-eye kernel.
- The new owner smoke/contract drives carry → reward → chase-settle → chase-
  walk and proves case 3 only after the post-behavior velocity reaches 10.

## Boundaries

This closes the owner-side eye decision, not the final renderer graph-node
mutation or display-list material selection. Other Tuxie effects/dialogs,
remaining NPC/puzzle/boss/save/audio/HUD/front-end/Metal 4 production, and
human acceptance gates remain open. No device, visual, physical controller,
audio, store, notarization, or clean-machine claim is made.

## Next

Continue the next reachable M20/M21 gameplay seam without creating branches or
pushing.
