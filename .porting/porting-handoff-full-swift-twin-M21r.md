# Full Swift Twin Handoff — M21r

## Status

M21r is complete locally as a bounded Big Bully-with-minions owner seam. The
bridge creates the three source-positioned small minions, records generation-
safe parent identity, propagates minion death to the parent's shared knockout
counter, and gates the 91-frame puzzle-jingle activation into a visible,
tangible landing route.

## Evidence

- Focused command: `script/test_bully_minion_owner.sh`
- Swift/C minion fingerprint:
  `bullyMinionOwnerFingerprint=0x4ff3b69000b7c356`
- Contract: source minion transforms, parent/child generation identity,
  minion unload/deletion delivery, three-KO parent counter, 91-frame activation,
  puzzle-jingle music presentation, and tangible/visible landing effects.
- Full regression matrix: `/tmp/sm64-modern-m21s-final-matrix.log`,
  `MATRIX_RESULT runs=195 failures=0` (M21r was included before M21s).
- Native build: `/tmp/sm64-modern-m21s-build.log`, `BUILD SUCCEEDED`.
- `git diff --check` passes.
- `rg -l '@unchecked Sendable' SM64Modern --glob '*.swift' | wc -l` reports 0.

## Boundaries

This checkpoint does not prove full minion interaction behavior, production
collision authority, Bowser arenas, durable reward progression, real
audio/camera/particle/renderer consumers, physical-device behavior, visual
review, or human acceptance.

## Next

Use the M21s Bully movement seam to validate production collision fixtures and
then continue Bowser arena breadth and M22 behavior-coverage closure.
