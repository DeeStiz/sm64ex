# SM64 Modern Full Swift Twin — M33lw Handoff

## Scope

M33lw adds the `bhvHauntedBookshelfManager` book-family manager. The typed
reducer and generation-safe bridge preserve three BookSwitch child allocation,
near/facing admission, sequence/timer gates, solved-sequence bookshelf-open
transition, in-room behavior, and manager retirement.

## Evidence

- Focused Swift/C Haunted Bookshelf Manager contract matches at
  `0x391346c3d3849a04`.
- Dispatch/three-switch/solved-open smoke, behavior manifest, Metal 4 source
  contract, engine runtime, live-route oracle, regenerated strict Swift 6
  Xcode Debug build, shell syntax, and `git diff --check` pass.
- Manifest: 534 rows, 438 Swift-owned, 96 C adapters,
  fingerprint `0x57fbaa14ce02790d`.

## Open gates

Fresh full-verifier rerun after this slice, physical visual/performance/thermal/
device acceptance, full 7,419-row qualification, remaining NPC/environment/
menu/camera/audio actors, whole-engine Swift authority, actual content/visual
parity, release, and human acceptance remain open. The preceding M33lo verifier
was `verify_rc=0` with Apple M5 Max Metal 4 frame-one/status-0 host evidence;
that run predates the recent cutscene and bookshelf-family slices.

## Next

Continue the remaining source-ordered C adapter families, adding focused
C↔Swift contracts and live shard coverage before closing M33.
