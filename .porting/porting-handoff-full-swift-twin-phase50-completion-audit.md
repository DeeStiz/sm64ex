# Full Swift Twin Handoff — Phase 50 Completion Audit

Date: 2026-08-21

## Scope and verdict

This is a fresh read-only completion audit after Phase 49 and the Phase 50
documentation reconciliation. It does not change source, generated manifests,
route ledgers, promotion state, credentials, toolchain selection, release
artifacts, or external state.

The Full Swift Twin is **not complete**. The retained `oracle_hook|input` row
is the only live-qualified route. Phase 49's native archive/lifecycle smoke is
valid general owner-thread evidence, but it is not Castle Inside area 2 and
contains no pendulum-owner qualification. The exact missing source-backed
entrypoint is an owner-thread level/area selector that runs the real
`LEVEL_CASTLE` level script through its existing `levelCommandAddr`, performs
the normal Mario-area transition to area 2, and exposes the resulting pendulum
object/callback without fabricating legacy globals or adding a second schema-4
sink. M34 and M35 remain blocked at their existing boundaries. No shipped,
visual-parity, or complete full-game Swift claim is admissible.

## Snapshot and counters

```text
behavior_rows=534 swift_value_owner=511 unmigrated_c_adapter=23
manifest_rows=7419 live_qualified=1 planned=7418
```

The retained `oracle_hook|input` row remains **1 of 7,419** live-qualified,
with 7,418 planned. Phase 49's `liveOracleTraceRecords=3151` across five
lifecycle ticks is general lifecycle coverage only and does not alter that
route counter.

## Completion matrix

| Requirement | Status | Current evidence boundary |
| --- | --- | --- |
| Every reachable behavior/system path has a Swift owner or documented unreachable compatibility leaf. | **INCOMPLETE** | The manifest remains 534 rows, 511 Swift value/owner routes, and 23 explicit C adapters. Focused owner/dispatch contracts and the Phase 44 seam do not prove whole-engine authority, lifetime, fallback, or unreachable-leaf closure. |
| Every reachable route shard has independent C/Swift schema-4 parity, terminal merge, and Debug/sanitizer/optimized reruns. | **INCOMPLETE** | Only `oracle_hook|input` is live-qualified. The Phase 46 source recipe has no native C owner trace; Phase 47 identifies the loader boundary; Phase 49's general lifecycle smoke is not a pendulum pair. The remaining 7,418 rows are open. |
| Native Castle Inside area-2 pendulum route has a source-backed owner-thread selection and independent C/Swift pair. | **BLOCKED** | The public lifecycle API owns `thread5_game_loop()`/`lifecycle_step()` but exposes no level/area selection or private `levelCommandAddr`; the bootstrap selects only `LEVEL_CASTLE_GROUNDS` or `LEVEL_BOB`. Direct `load_area(2)` would require fabricated or out-of-band `gCurrentArea`, `gMarioSpawnInfo`, `gMarioObject`, object lists, surfaces, and audio/effect ownership. |
| Strict Swift 6 diagnostics, ownership audits, sanitizers, normal rebuild, and required Metal 4 validation cover the twin. | **INCOMPLETE** | Focused source/ABI/owner contracts pass, but no all-path ownership audit, sanitizer matrix, post-sanitizer normal rebuild, or full-twin qualification record exists here. |
| Metal 4 production evidence proves visible presentation, resize/pause, archive reuse, GPU inspection, visual/reference parity, cadence, memory, and thermal/device acceptance. | **BLOCKED** | M34 remains locked/headless and bounded to three presents with clear-only black fetched attachments and unproven archive reuse. This is structural diagnostic evidence only. |
| Developer ID signed/notarized/stapled artifacts and clean-machine Gatekeeper checks exist. | **BLOCKED** | M35 remains fail-closed on exactly two prerequisites: an authorized Developer ID Application identity/private key and one supported `notarytool` authentication mode. No artifact or clean-machine record exists. |
| Fresh-save physical/human acceptance covers controls, camera, collision, audio, haptics, visuals, menus, ending, and recovery. | **MISSING** | No dated physical matrix or fresh-save human-acceptance artifact exists; automated source, build, route, and capture evidence cannot substitute for human observation. |

## Phase 43–49 authority audit

- **Global state (Phase 43):** the native `gGlobalTimer`, live
  level/area/act/course lifecycle values, and shared `gRandomSeed16` still have
  no single source-backed Swift owner-thread schema-4 emitter.
- **Decorative owner/dispatch (Phases 44–45):** the explicitly configured
  Swift owner seam and central dispatch are source-backed and trace-producing;
  the unconfigured identity-only path remains trace-silent. These seams do not
  imply that live level content is loaded.
- **Source recipe/native C boundary (Phases 46–47):** the real Castle Inside
  area-2 behavior, level-script, collision, and room inputs are available to a
  Swift diagnostic recipe, but a C pair is blocked by monolithic
  `data/behavior_data.c`, `gCurrentObject`, and legacy level/surface/audio
  ownership. No synthetic loader, callback override, or schema-4 sink was
  added.
- **Documentation/native baseline (Phase 48–49):** Phase 48 preserved those
  boundaries in the public/status ledger. Phase 49's strict native archive and
  lifecycle smoke passed with 3,151 records over five ticks, domains
  `0x00001ff7`, coverage fingerprint `0x5ad92028e4bd8daf`, and 62 coverage
  entries. The archive's `load_area`, `gAreaData`, `gCurrentArea`,
  `gMarioSpawnInfo`, and pendulum symbols are not proof of owner initialization.

## External boundaries and exact unblocks

- **Pendulum route:** add one native lifecycle owner-thread entrypoint that
  selects `LEVEL_CASTLE`, drives the compiled level script through the existing
  command pointer, performs the normal Mario-area transition to area 2, and
  exposes the real pendulum object/callback through the existing parity sink.
  Then capture at least two independent C and Swift pendulum ticks, compare
  complete schema-4 traces byte-for-byte, and only then run executor,
  replay/tamper, worker/merge, and promotion fences.
- **M34:** provide an unlocked, awake, visible GUI/compositor session; recapture
  post-resume non-clear reference pixels, repeated presentation, archive reuse,
  and independent device/performance/thermal evidence.
- **M35:** provide the authorized Developer ID identity/private key and one
  supported `notarytool` authentication mode; run the existing fail-closed
  archive/export/notarize/staple flow, verify Gatekeeper on a separate clean
  machine, and record physical/human acceptance separately.

## Focused checks performed

Passed during this bounded Phase 50 docs pass:

- `./script/test_behavior_manifest.sh` — 534 rows, 511 Swift value/owner
  rows, 23 explicit C adapters, fingerprint `0x5e5d8c00a7fab8a3`.
- `./script/test_route_shards.sh` — `inventory=7419 shards=7419
  status=planned`.
- `./script/test_route_shard_merge.sh`.
- `./script/test_route_shard_worker_result.sh`.
- `./script/test_route_shard_replay.sh`.
- Markdown target/link existence checks for the reconciled public/status docs,
  Phase 43–50 handoffs, and their linked continuation references.
- `git diff --check`.

The Phase 49 handoff separately records the strict native-core build and
`./script/test_oracle_lifecycle_record.sh` result. Existing Phase 44–48
owner, dispatch, source-recipe, and Metal-contract checks remain recorded in
their respective handoffs and were not broadened or rerun in this bounded docs
pass.

These focused checks validate source contracts, counters, documentation links,
and fail-closed evidence boundaries only. They do not close route execution,
visible Metal production, physical performance/thermal, release,
clean-machine, or human-acceptance gates.

No commit was created; the parent agent owns review and commit.
