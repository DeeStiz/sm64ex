# Full Swift Twin Handoff — Phase 85f93 JRB Treasure-Chest Seam Execute

Date: 2026-08-23

## Verdict

**SEAM IMPLEMENTED / REAL ROUTE BLOCKED / NO ADMISSION.** The private
schema-4 observer seam now sits at the source-owned JRB treasure-chest root,
bottom, and top behavior boundaries. It copies semantic root/bottom/top
identities, source child ordinals 1 through 4, authored scalar state,
interaction outcomes, and effect intents without exposing native pointers.
The independent Swift mirror validates the source identities, JRB variant,
root source order, complete child ordinal set, record shape, and canonical
schema-4 bytes.

The focused native probe followed the ordinary lifecycle only. It started at
Castle grounds under the normal lifecycle configuration and did not reach the
authored Castle Inside JRB painting nodes `0x09`, `0x0A`, or `0x0B`, so it
returned the required fail-closed exit `77` and removed the header-only trace.
No direct JRB load, chest helper call, child injection, variant substitution,
coordinate selection, or synthetic trace was used.

## Owned implementation

- `src/pc/sm64_modern_treasure_chest_jrb_route_identity.h` — private route
  identities, fixed-width root/bottom/top inputs and schema-4 receipt.
- `src/pc/sm64_modern_treasure_chest_jrb_route_identity.c` — source-bound
  value receipt validation and script/object/collision/effect record emission.
- `src/game/behaviors/treasure_chest.inc.c` — narrow owner-boundary observers;
  the source child scan is ordered by the authored behavior parameter and
  copies positions/yaw as values only.
- `tests/sm64_modern_treasure_chest_jrb_route_pair_contract.c` — real
  owner-thread lifecycle probe and fail-closed root/bottom/top reachability
  check.
- `tests/sm64_modern_treasure_chest_jrb_route_swift_smoke.swift` — independent
  Swift 6 schema-4 mirror with tamper, partial, wrong-variant, duplicate,
  fixture-only, and single-artifact rejection paths.
- `script/test_treasure_chest_jrb_route_pair.sh` — strict Swift/C build,
  source route checks, Debug/ASan/Release/rerun parity path, and negative
  fences; blocked reachability exits `77` before admission work.

## Validation evidence

Passed:

- Debug `native-core` build with the new observer source.
- Strict Swift 6 compilation of `OracleTrace.swift` plus the independent
  JRB route mirror.
- C11 `-Wall -Wextra -Werror` compilation of the focused lifecycle probe.
- `bash -n script/test_treasure_chest_jrb_route_pair.sh`.
- Focused route script reached its explicit blocked branch:
  `SM64 Modern JRB treasure-chest route pair blocked reachability=0` and
  `exit=77`; the native log reported `level=1 area=1 roots=0 bottoms=0 tops=0`.
- The blocked probe removed its header-only trace (`no_trace_on_block=1`).

Not reached because the authored painting lifecycle is currently blocked:

- Native receipt records and positive C/Swift pairing.
- ASan, optimized Release, and fresh-rerun byte parity.
- Positive partial/wrong-variant/duplicate/single-artifact/rerun fence run.
- Any isolated admission, manifest/report/ledger update, or central merge.

The designated route row `0x246e8a98cbad9a7a` remains planned and no
canonical or backup artifact was opened for write.
