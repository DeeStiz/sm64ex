# Full Swift Twin Handoff — M21t

## Status

M21t is complete locally as a bounded Bowser shockwave value/owner seam. The
pointer-free Swift kernel reproduces the source expansion, opacity cadence,
late fade, strict Mario-distance interaction windows, and deletion fence. The
owner bridge attaches the shockwave to generation-safe records, routes the
Mario interaction bit through the owner thread, and unloads the wave through
the scheduler pool. The default C-compatible path remains available; this
slice does not claim full Bowser authority.

## Evidence

- Focused command: `script/test_bowser_shockwave.sh`
- Swift/C fingerprint:
  `bowserShockWaveFingerprint=0x4dbf62c6e6d1688d`
- Contract: initial expansion, three-frame opacity decrement, late fade,
  strict inner/outer Mario ring windows, owner-thread interaction mutation,
  generation-safe deletion, and pool unload.
- Full matrix: `/tmp/sm64-modern-m21t-final-matrix.log`,
  `MATRIX_RESULT runs=196 failures=0`.
- Native build: `/tmp/sm64-modern-m21t-build.log`, `BUILD SUCCEEDED` after
  `xcodegen generate --spec project.yml`.
- `git diff --check` passes.
- `rg -l '@unchecked Sendable' SM64Modern --glob '*.swift' | wc -l` reports 0.

## Boundaries

The fixture proves one shockwave behavior and its owner mutation route, not the
full Bowser controller, arena collision, arena camera/cutscene, boss music
state machine, reward/warp transitions, or real renderer/audio consumers.
Production collision authority, Metal visual parity, physical-device behavior,
controller/audio feel, and human acceptance remain open.

## Next

Continue M21 with Bowser controller/arena breadth and remaining Chief Chilly
routes, then close M22's reachable behavior/callback inventory before starting
the M23 save/config/HUD/front-end replacement phase. Keep each route gated by
strict Swift/C fingerprints, owner-thread records, isolated native builds, and
the complete regression matrix.
