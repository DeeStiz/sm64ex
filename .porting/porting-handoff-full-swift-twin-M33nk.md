# SM64 Modern Full Swift Twin — M33nk Handoff

M33nk adds `bhvSnowmansHead` as a Swift 6 value/owner route. The reducer
preserves the authored 0.7 scale and gravity/friction/buoyancy initialization,
dialog action gates, falling collision transition, -994 Y clamp,
explosion/jingle edge, Mario-push intent, and post-dialog mist/star intent.

Focused contract `0x2eb225e676e807a4` matches between Swift and C. Explicit
dispatch, aggregate behavior smoke, engine runtime, full live-route oracle,
Metal 4 source contract, XcodeGen regeneration, shell syntax, and
`git diff --check` pass. The behavior manifest is `0x315aaf6ce5ab1f31` with
534 rows (`488` Swift owners, `46` C adapters).

The latest complete host verifier remains blocked by LaunchServices
`kLSNoExecutableErr (-10827)` after a successful strict Swift 6 build; the
direct executable diagnostic still exits `134` during AppKit registration.
The retained complete host proof is M33nc. Remaining route shards, sanitizer,
host recovery, physical visual/performance/thermal/device, release,
audio/effect parity, and human acceptance remain open.

Next safe slice: continue the remaining compact behavior ledger, starting with
the paired Snowman bottom owner or another isolated parent/child route. Re-run
focused C parity, aggregate dispatch, runtime/live/Metal gates, and a full
verifier after a meaningful batch; do not treat fixture or Simulator evidence
as physical parity.
