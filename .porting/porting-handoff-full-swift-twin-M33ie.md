# SM64 Modern Full Swift Twin — M33ie Handoff

## Scope

M33ie extends the Swift coin owner through `bhvThreeCoinsSpawn`,
`bhvTenCoinsSpawn`, and `bhvSingleCoinGetsSpawned`. Formation parents spawn
exactly three or ten level-list children, deactivate after their default-list
pass, and preserve the scheduler's next-pass child ordering.

## Evidence

- Focused C↔Swift formation contract: `coinFormationFingerprint=0xcb80b3715d3a6b4a`.
- Dispatch child-order smoke, engine runtime, live-route oracle, route-shard
  replay, timebase, manifest, Metal source, shell, and hygiene gates pass.
- Manifest: `0xda5e7977cf2e5d16`, 534 rows, 370 Swift routes, 164 C adapters.
- Regenerated strict Swift 6 Xcode Debug build passes at
  `build/sm64-modern-m33ie-xcode` with `** BUILD SUCCEEDED **`.

## Open gates

Post-slice full verifier rerun, all 7,419 route shards, whole-engine Swift
authority, native/device launch, actual content/visual parity, GPU/
performance/thermal, release, and human acceptance remain open. The host
LaunchServices failure remains `kLSNoExecutableErr (-10827)` before AppKit.

## Next

Continue dynamic collision/global actor families and retain independent
C↔Swift contracts for each migration slice.
