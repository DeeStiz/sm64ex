# Full Swift Twin Handoff — Phase 85f78 Spindel Seam Execute

Date: 2026-08-23

## Verdict

**SEAM IMPLEMENTED / ROUTE FAIL-CLOSED AT REAL REACHABILITY.** The private
source-owned Spindel receipt seam, semantic `bhvSpindel` identity, independent
schema-4 Swift mirror, focused C contract, and route-pair harness are present
for planned shard `0xdc93743116807bec`. The ordinary host lifecycle remained
in Castle Inside area 1 for the bounded run, so no native receipt was emitted;
the harness correctly reported exit 77. No direct SSL load, helper call,
object injection, coordinate selection, synthetic trace, manifest mutation,
ledger mutation, or canonical report write was performed.

## Source seam

- The route constants retain the frozen planned-row source/owner identities
  `0x4b8e5a734fe76b34` / `0x4b4378861cfabced`; semantic `bhvSpindel` remains
  `0x789601af843158ee` and is independent of the shared Swift dispatch lane.
- `src/game/behaviors/spindel.inc.c` copies only fixed-width before/after
  timer, phase, direction, position, pitch, velocity, effect, and named
  collision values at the existing source owner boundary. The source reducer,
  sound, camera shake, and behavior-script collision load remain authoritative.
- `src/pc/sm64_modern_spindel_route_identity.[ch]` publishes schema-4 script,
  object-bound semantic identity, collision, and effect receipts for the
  source-authored SSL area-2 `script_func_local_4` ordinal-5 Spindel.
- `src/pc/sm64_modern_gameplay_parity.c` maps `bhvSpindel` to the semantic
  source-name hash `0x789601af843158ee`; no linked behavior pointer is used.

## Pair and negative fences

- `tests/sm64_modern_spindel_route_pair_contract.c` starts from the normal
  owner-thread lifecycle and selects only the first authored `bhvSpindel` in
  SSL area 2. It requires script/object/collision/effect receipts and rejects
  helper/direct-load/injection paths by source fence.
- `tests/sm64_modern_spindel_route_swift_smoke.swift` is a value-only schema-4
  mirror with source tuple/reducer checks and distinct-artifact comparison.
- `script/test_spindel_route_pair.sh` checks Castle SSL painting nodes `0x0f`,
  `0x10`, `0x11`, SSL area-1 warp `0x14`, and the authored area-2 object. It
  includes Debug/ASan/Release/rerun parity and tamper, partial, wrong-subject,
  generic shared-tumbling-bridge identity, fixture-only, duplicate,
  single-artifact, and persistent-rerun fences.

## Validation

```text
bash -n script/test_spindel_route_pair.sh                         passed
Swift 6 strict route mirror compile                                    passed
C11 -Wall -Wextra -Werror focused contract compile                     passed
make SM64_MODERN_NATIVE=1 DEBUG=1 ... native-core                     passed
bash script/test_spindel_route_pair.sh                                exit 77
```

The route-pair run logged:

```text
spindel_route_unreachable level=1 area=1 spindels=0
SM64 Modern Spindel route pair blocked reachability=0
... first_spindel_receipt=absent sibling_substitution=0 coordinate_matching=0 fixture_only=0
... admission=0 canonical_ledger_mutation=0 manifest_mutation=0 exit=77
```

Because authored reachability was absent, ASan/Release/rerun byte parity and
Swift negative execution were not claimed; they remain ready behind the same
fail-closed gate.

## Next step

Provide a normal source-authored Castle Inside SSL painting traversal into SSL
area 1 and its `0x14` area-2 warp in the host lifecycle, then rerun the route
harness. Only a positive source receipt should unlock the independent
Debug/ASan/Release/rerun pair and any separately authorized isolated admission.
M34/M35, physical, and human acceptance remain independent gates.
