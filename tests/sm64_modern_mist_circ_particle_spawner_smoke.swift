import Foundation
private let fnvOffset: UInt64 = 1_469_598_103_934_665_603
private let fnvPrime: UInt64 = 1_099_511_628_211
private func hash(_ seed: UInt64, _ value: UInt64) -> UInt64 { var result = seed; for index in 0..<8 { result ^= (value >> UInt64(index * 8)) & 0xff; result &*= fnvPrime }; return result }
@main struct SM64MistCircParticleSpawnerSmoke { static func main() { let output = SM64MistCircParticleSpawnerBehavior.update(.init(position: .init(x: 10, y: 20, z: -4), timer: 0, activeParticleFlags: 0x8000, particleFlag: 0x8000, seeds: [.init(randomScaleUnit: 0.5, randomYaw: 0, randomForwardUnit: 0.4)])); precondition(output.children.count == 1 && output.children[0].position == .init(x: 10, y: 40, z: -4) && output.children[0].velocity == .init(x: 0, y: 0, z: 12) && output.children[0].scale == 3.075 && output.clearParticleFlag && output.shouldSpawnChildren && !output.shouldDeactivate); var fingerprint = fnvOffset; fingerprint = hash(fingerprint, UInt64(output.children.count)); fingerprint = hash(fingerprint, UInt64(output.children[0].position.y.bitPattern)); fingerprint = hash(fingerprint, UInt64(output.children[0].velocity.z.bitPattern)); fingerprint = hash(fingerprint, UInt64(output.children[0].scale.bitPattern)); fingerprint = hash(fingerprint, output.clearParticleFlag ? 1 : 0); print(String(format: "mistCircParticleSpawnerFingerprint=0x%016llx", fingerprint)); print("SM64 Modern mist-circle particle spawner smoke passed") } }
