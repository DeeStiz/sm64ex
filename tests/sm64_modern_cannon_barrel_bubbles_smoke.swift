import Foundation
private let fnvOffset: UInt64 = 1_469_598_103_934_665_603
private let fnvPrime: UInt64 = 1_099_511_628_211
private func hash(_ seed: UInt64, _ value: UInt64) -> UInt64 { var result = seed; for index in 0..<8 { result ^= (value >> UInt64(index * 8)) & 0xff; result &*= fnvPrime }; return result }
@main struct SM64CannonBarrelBubblesSmoke { static func main() { let output = SM64CannonBarrelBubblesBehavior.update(.init(position: .zero, parentPosition: .init(x: 10, y: 20, z: -4), relativePosition: .zero, parentFaceYaw: 0x1000, parentMovePitch: 0x2000, parentAction: 0, accumulatedDistance: 0, forwardVelocity: 0, cannonActive: true)); precondition(output.position == .init(x: 10, y: 20, z: -4) && output.moveYaw == 0x1000 && output.movePitch == 0x6000 && output.facePitch == 0x2000 && output.forwardVelocity == 35 && output.spawnBomb); var fingerprint = fnvOffset; fingerprint = hash(fingerprint, UInt64(output.moveYaw)); fingerprint = hash(fingerprint, UInt64(output.movePitch)); fingerprint = hash(fingerprint, UInt64(output.forwardVelocity.bitPattern)); fingerprint = hash(fingerprint, output.spawnBomb ? 1 : 0); print(String(format: "cannonBarrelBubblesFingerprint=0x%016llx", fingerprint)); print("SM64 Modern cannon-barrel bubbles smoke passed") } }
