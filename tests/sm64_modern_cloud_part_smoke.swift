import Foundation
private let fnvOffset: UInt64 = 1_469_598_103_934_665_603
private let fnvPrime: UInt64 = 1_099_511_628_211
private func hash(_ seed: UInt64, _ value: UInt64) -> UInt64 { var result = seed; for index in 0..<8 { result ^= (value >> UInt64(index * 8)) & 0xff; result &*= fnvPrime }; return result }
@main struct SM64CloudPartSmoke { static func main() { let output = SM64CloudPartBehavior.update(.init(parentCenterX: 100, parentCenterY: 50, parentPositionZ: -20, parentFaceYaw: 0, parentScale: 3, partIndex: 0, globalFrame: 0, parentUnloading: false)); precondition(output.position == .init(x: 104, y: 76, z: 34) && output.scaleX == 2 && output.scaleY == 2 && output.scaleZ == 2 && !output.shouldDeactivate); var fingerprint = fnvOffset; fingerprint = hash(fingerprint, UInt64(output.position.x.bitPattern)); fingerprint = hash(fingerprint, UInt64(output.position.y.bitPattern)); fingerprint = hash(fingerprint, UInt64(output.position.z.bitPattern)); fingerprint = hash(fingerprint, UInt64(output.scaleX.bitPattern)); fingerprint = hash(fingerprint, UInt64(output.scaleY.bitPattern)); print(String(format: "cloudPartFingerprint=0x%016llx", fingerprint)); print("SM64 Modern cloud part smoke passed") } }
