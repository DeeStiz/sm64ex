import Foundation
private let fnvOffset: UInt64 = 1_469_598_103_934_665_603
private let fnvPrime: UInt64 = 1_099_511_628_211
private func hash(_ seed: UInt64, _ value: UInt64) -> UInt64 { var result = seed; for index in 0..<8 { result ^= (value >> UInt64(index * 8)) & 0xff; result &*= fnvPrime }; return result }
@main struct SM64TreeParticleSpawnerSmoke { static func main() { let output = SM64TreeParticleSpawnerBehavior.update(.init(position: .zero, timer: 0, activeParticleFlags: 0x2000, particleFlag: 0x2000, snowMode: false, spawnDecision: 0.2, randomScale: 0.5, randomYaw: 0x4000, randomForwardUnit: 0.4, randomVerticalUnit: 0.5, randomFacePitch: 100, randomFaceRoll: 200)); precondition(output.childKind == .leaf && output.childScale == 1.5 && output.childForwardVelocity == 7 && output.childVelocityY == 7.5 && output.clearParticleFlag && output.shouldSpawnChild && !output.shouldDeactivate); var fingerprint = fnvOffset; fingerprint = hash(fingerprint, UInt64(output.childScale.bitPattern)); fingerprint = hash(fingerprint, UInt64(output.childForwardVelocity.bitPattern)); fingerprint = hash(fingerprint, UInt64(output.childVelocityY.bitPattern)); fingerprint = hash(fingerprint, output.clearParticleFlag ? 1 : 0); fingerprint = hash(fingerprint, output.shouldSpawnChild ? 1 : 0); print(String(format: "treeParticleSpawnerFingerprint=0x%016llx", fingerprint)); print("SM64 Modern tree particle spawner smoke passed") } }
