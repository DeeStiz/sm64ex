import Foundation
private let fnvOffset: UInt64 = 1_469_598_103_934_665_603
private let fnvPrime: UInt64 = 1_099_511_628_211
private func hash(_ seed: UInt64, _ value: UInt64) -> UInt64 { var result = seed; for index in 0..<8 { result ^= (value >> UInt64(index * 8)) & 0xff; result &*= fnvPrime }; return result }
@main struct SM64BreakBoxTriangleSmoke { static func main() { let output = SM64BreakBoxTriangleBehavior.update(.init(position: .init(x: 10, y: 20, z: -4), moveYaw: 0, facePitch: 0, faceYaw: 0, faceRoll: 0, angleVelocityPitch: 0x100, angleVelocityYaw: 0x200, angleVelocityRoll: 0x300, forwardVelocity: 5, velocityY: 15, gravity: -1, timer: 0)); precondition(output.position == .init(x: 10, y: 34, z: 1) && output.facePitch == 0x100 && output.faceYaw == 0x200 && output.faceRoll == 0x300 && !output.shouldDeactivate); var fingerprint = fnvOffset; fingerprint = hash(fingerprint, UInt64(output.position.y.bitPattern)); fingerprint = hash(fingerprint, UInt64(output.position.z.bitPattern)); fingerprint = hash(fingerprint, UInt64(output.facePitch)); fingerprint = hash(fingerprint, UInt64(output.faceYaw)); print(String(format: "breakBoxTriangleFingerprint=0x%016llx", fingerprint)); print("SM64 Modern break-box triangle smoke passed") } }
