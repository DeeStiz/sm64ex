import Foundation
private let fnvOffset: UInt64 = 1_469_598_103_934_665_603
private let fnvPrime: UInt64 = 1_099_511_628_211
private func hash(_ seed: UInt64, _ value: UInt64) -> UInt64 { var result = seed; for index in 0..<8 { result ^= (value >> UInt64(index * 8)) & 0xff; result &*= fnvPrime }; return result }
@main struct SM64TriangleParticleSmoke { static func main() { let output = SM64TriangleParticleBehavior.update(.init(position: .zero, marioPosition: .init(x: 100, y: 50, z: -20), marioYaw: 0, timer: 0, moveYaw: 0, forwardVelocity: 25, velocityY: 14, gravity: 0, scale: 1, lifetime: 6)); precondition(output.position == .init(x: 100, y: 124, z: 105) && output.scale == 1.28 && output.animationState == 5 && !output.shouldDeactivate); var fingerprint = fnvOffset; fingerprint = hash(fingerprint, UInt64(output.position.x.bitPattern)); fingerprint = hash(fingerprint, UInt64(output.position.y.bitPattern)); fingerprint = hash(fingerprint, UInt64(output.position.z.bitPattern)); fingerprint = hash(fingerprint, UInt64(output.scale.bitPattern)); fingerprint = hash(fingerprint, UInt64(output.animationState)); print(String(format: "triangleParticleFingerprint=0x%016llx", fingerprint)); print("SM64 Modern triangle particle smoke passed") } }
