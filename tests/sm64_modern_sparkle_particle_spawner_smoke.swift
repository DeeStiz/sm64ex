import Foundation
private let fnvOffset: UInt64 = 1_469_598_103_934_665_603
private let fnvPrime: UInt64 = 1_099_511_628_211
private func hash(_ seed: UInt64, _ value: UInt64) -> UInt64 { var result = seed; for index in 0..<8 { result ^= (value >> UInt64(index * 8)) & 0xff; result &*= fnvPrime }; return result }
@main struct SM64SparkleParticleSpawnerSmoke { static func main() { let output = SM64SparkleParticleSpawnerBehavior.update(.init(position: .init(x: 10, y: 20, z: -4), timer: 0, animationState: -1, activeParticleFlags: 0x800, particleFlag: 0x800, randomOffset: .init(x: 1, y: -2, z: 3))); precondition(output.position == .init(x: 11, y: 18, z: -1) && output.animationState == 0 && output.graphYOffset == 25 && output.clearParticleFlag && !output.shouldDeactivate); var fingerprint = fnvOffset; fingerprint = hash(fingerprint, UInt64(output.position.x.bitPattern)); fingerprint = hash(fingerprint, UInt64(output.position.y.bitPattern)); fingerprint = hash(fingerprint, UInt64(output.position.z.bitPattern)); fingerprint = hash(fingerprint, UInt64(output.animationState)); fingerprint = hash(fingerprint, output.clearParticleFlag ? 1 : 0); print(String(format: "sparkleParticleSpawnerFingerprint=0x%016llx", fingerprint)); print("SM64 Modern sparkle particle spawner smoke passed") } }
