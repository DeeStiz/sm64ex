SM64 Modern native macOS — nightly continuation update (2026-08-23)
43.) Record Phase 57's native Castle pendulum parity repair: schema-4
     object-domain snapshots now retain the real slot-37 actor state, six
     native run/header fingerprints are nonzero, and the 64-step pair remains
     blocked only by the still-missing native effect domain and exact C/Swift
     record/header parity. No synthetic sound or route admission is allowed.
44.) Record Phase 58's authored Castle Inside area-2 `WARP_NODE(0x35)` route.
     The normal owner-thread warp path now places Mario and the pendulum in
     room 5 with the pendulum render-active (`graph_flags=0x21`); the sound
     threshold still lands on a held native step, so effect parity remains
     blocked and no route ledger mutation occurs.
45.) Record Phase 59's independent pendulum re-pair: native emits domains
     `3,6,7` while Swift emits `3,6,7,12`, with `native_records=1057`,
     `swift_records=1095`, and a first domain-3 record mismatch. Promotion is
     not attempted; room/render evidence is retained as lifecycle evidence.
46.) Reconcile Phase 60 current docs and completion audit. Keep authoritative
     counters at 534 behavior rows (511 Swift owners, 23 C adapters) and
     1/7,420 live-qualified route shards with 7,419 planned. M34 visible-layer,
     post-resume, archive-reuse, visual/FPS/memory/thermal; M35 Developer ID,
     notary, signed/stapled artifact, and clean-machine; and human 120-star
     acceptance gates remain explicitly open.
47.) Record Phase 61's M34 production audit. The canonical production harness
     stops before app launch on `EngineRuntime.swift:189/:366` because
     `SM64ModernStatus` and `Int32` do not match. The retained `gpudebug`
     trace is structural/clear-only and adds no visible-layer, post-resume,
     archive-reuse, FPS, GPU-time, memory, or thermal evidence.
48.) Record Phase 62's M35 release preflight. Ordinary Xcode 26.6 works via an
     invocation override, but no Developer ID Application identity/private key
     or notary authentication is available; no archive/export/DMG/ZIP/staple/
     Gatekeeper, clean-machine, or human result exists.
49.) Close Phase 64 documentation indexes without changing source or route
     admission. Preserve 534 behavior rows (511 Swift owners, 23 C adapters),
     7,420 total route shards (1 live, 7,419 planned), separate 95.693% and
     0.013477% indicators, and the conservative 0% full-goal floor.
50.) Record Phase 65's fixed-width EngineRuntime status conversion. The
     `SM64ModernStatus`/`Int32` boundary now builds under the beta Release
     toolchain; route, M34, M35, and human acceptance claims remain open.
51.) Record Phase 66's fixed-build M34 rerun. Release build passed, but the
     harness failed closed at `scheduler_dropped_steps=65` on a host with
     sleeping displays, three presents, and no capture pass.
52.) Record Phase 67's stable-Xcode M35 preflight. Contract checks pass, while
     Developer ID Application identity/private key and notary authentication
     remain unavailable; no distribution artifact or acceptance claim exists.
53.) Record Phase 67c's HUD arithmetic type-check fix with unchanged C↔Swift
     fingerprints; retain the stable AVFAudio SDK diagnostics as the next
     bounded build blocker.
54.) Record Phase 67d's macOS 26/27 AVFAudio compatibility. Stable generic
     Release build and audio contracts pass; the local product remains
     unsigned and not distribution-ready.
55.) Record Phase 67b's latest stable M34 rerun: the unchanged harness fails
     closed at `scheduler_dropped_steps=63` with the screen locked, three
     presents, and no new capture.
56.) Reconcile Phase 70 README, SM64 Modern docs, CHANGES, both goal ledgers,
     and porting memory. Preserve 534/511/23 and 7,420 total/1 live/7,419
     planned; keep route pairing, M34, M35, clean-machine, and human gates
     fail-closed; link every current handoff and record the next awake-host,
     route, signing/notary, and human-acceptance sequence.
57.) Record Phase 71's camera-state route audit. The existing input-only row
     still pairs independently; full-route coverage fails before a camera
     trace, and Mario-face/progression contracts remain source/fixture evidence.
     No second live shard was admitted; the ledger remains 1/7,420.
58.) Record Phase 72's full-route coverage repair: the real first-input receipt
     is retained so the composite source trace reaches C replay.
59.) Record Phase 73's independent C sidecar extension: all nine full-route
     records replay exactly and tamper rejection passes, but the composite has
     no canonical manifest row, so live admission remains 1/7,420.
60.) Record Phase 75's post-SDK M35 preflight: stable generic Release and the
     readiness/distribution contracts pass, while Developer ID/notary,
     artifact, clean-machine, and human gates remain open.
61.) Record Phase 74's read-only M34 host gate: the console is locked and both
     online displays are asleep, so `m34_host_ready=0` and no wake/unlock or
     power-state mutation is attempted.
62.) Record Phase 76's read-only route-admission triage: all 7,420 manifest rows
     were scanned against the retained nine-record composite trace; 6,206
     fully key-covered candidates were listed, but 0 were admissible because
     the trace is unbound to a manifest identity and per-row independent C/
     Swift evidence is missing. Preserve `promoted_rows=0`, `ledger_mutated=0`,
     and live admission at 1/7,420.
63.) Reconcile Phase 77 documentation and handoff links. Preserve 534 behavior
     rows (511 Swift owners, 23 explicit C adapters), 7,420 route shards
     (1 live, 7,419 planned), the `IOConsoleLocked=Yes`/`session_locked=Yes`
     host evidence, stable generic Release/M35 contract pass, and the open
     awake-host, independent-route, signing/notary, clean-machine, and human
     acceptance gates. No source or ledger mutation is implied.
64.) Reconcile Phase 79's Mario-state route attempt. The source-backed native
     harness compiled and initialized, but real lifecycle steps returned
     `status=4` and the parity oracle ended `status=10` before required
     domain-2/state records; the prototype was removed, with no Swift pair,
     manifest/ledger mutation, route promotion, or partial contract retained.
     Preserve 534/511/23, 7,420/1/7,419, the locked/asleep M34 host, and the
     Developer ID/notary, signed-artifact, clean-machine, and human-acceptance
     blockers. Link the Phase 79 and Phase 80 handoffs and keep the native
     owner/parity repair ahead of independent C/Swift route admission.

65.) Record Phase 81's ready-host M34 attempt: the read-only host gate passed,
     but the unchanged API/shader-validation profile failed closed at
     `scheduler_dropped_steps=72` before capture. Preserve the failed artifact
     and do not claim M34 acceptance.
66.) Record Phase 82a's bounded scheduler fix: removing the synchronous
     owner-thread pipeline wait produced a zero-drop validation profile while
     capture overhead remained separate. Link the scheduler-cadence handoff.
67.) Record Phase 82b's stable-Xcode M34 reproducibility: validation/capture
     and noninteractive `gpudebug` structural checks passed; capture overhead,
     screenshot/reference pixels, sustained performance, thermal, and physical
     acceptance remain separate gates.
68.) Record Phase 82c's M35 distribution preflight: readiness contracts pass,
     but no Developer ID Application identity/private key or supported
     `notarytool` authentication exists. No signed/stapled artifact, clean
     machine, or human result exists.
69.) Record Phase 82d/82e archive and capture boundaries: normal validation
     proves binary archive load/reuse, while the capture interaction failed
     until a bounded capture-specific bypass was implemented; no failed capture
     was promoted as evidence.
70.) Record Phase 82f's capture archive bypass: the unchanged M34 two-pass
     harness passed validation/archive reuse, capture, resize/pause,
     post-resume, and noninteractive `gpudebug` structural inspection. The
     capture profile bypasses archive loading and remains subject to capture
     overhead; visual parity is unclaimed.
71.) Record Phase 84a's GPU attachment inspection: static trace enumeration
     found 515 render passes and 28,216 draws, but XPC replay interruption
     prevented attachment PNGs and any non-clear-pixel/reference-parity claim.
72.) Record Phase 84b's bounded sustained-performance attempt: two 3,600-step
     native Release profiles reached zero scheduler/audio drops at about
     59.94/59.96 Hz with bounded RSS; Instruments supplied bounded encoder,
     allocation, and nominal thermal observations only, not a 10/30-minute
     soak or physical feel/thermal closure.
73.) Reconcile Phase 84c documentation across README, SM64 Modern docs, both
     goal ledgers, `CHANGES`, and porting memory. Preserve 534/511/23,
     7,420/1/7,419, M34 archive/validation/capture/`gpudebug` evidence, the
     Phase 84a XPC pixel blocker, Phase 84b cadence/thermal boundaries, and
     the M35 Developer ID/notary/clean-machine/human blockers. Link Phases
     81, 82a–82f, and 84a–84c; no source, route ledger, credential, or release
     mutation is implied.
74.) Record Phase 85a/b's Mario-state owner repair and exact C/Swift/ASan
     schema-4 parity. The native lifecycle now retains 38 domain-2 records
     across ticks 2 and 3, with the first source-backed divergence repaired;
     route admission remained separate until the canonical row gate.
75.) Record Phase 85c's canonical Mario-state admission. The independent
     C/Swift/ASan traces passed exact byte parity, tamper and rerun fences, and
     one `oracle_hook|mario_state` row (`0x88d04246f94ce9f8`) moved to terminal
     passed without fixture or manifest mutation.
76.) Record Phase 85d–85f's camera route pair and source-backed repairs. The
     focus and camera-position/Lakitu path now matches all 14 schema-4 records
     byte-for-byte; the separate camera admission gate remained fail-closed
     until Phase 85g.
77.) Record Phase 85g's canonical camera-state admission. The independent
     C/Swift/ASan traces passed exact parity, tamper and rerun fences, and one
     `oracle_hook|camera_state` row (`0x4eb19b71d76be0d4`) moved to terminal
     passed without fixture or manifest mutation.
78.) Record Phase 85h's cumulative ledger merge. The authoritative manifest
     remains 7,420 rows; the cumulative report has 3 terminal passed rows and
     7,417 planned, with report SHA-256
     `d9b9927ebf0e49d9ba9c0bf62dab322fddd43e7899642f0716071a1a948c4312`.
     M34 pixel/soak/direct-display, M35 Developer ID/notary/clean-machine, and
     human acceptance remain open.
79.) Record Phase 85i's reconciliation of Phase 85a–85h across the public docs
     and ledgers. Link every
     scoped handoff, preserve the cumulative 3/7,420 route truth and
     7,417 planned rows, and keep all remaining route, Metal visual/soak,
     distribution, clean-machine, and human gates fail-closed. No source,
     manifest, route ledger, credential, or release mutation is implied.
80.) Record Phase 85j's source-backed global-state owner publication boundary:
     the native C lifecycle publishes the real timer/level/area/act/course/RNG
     snapshot synchronously to the Swift owner mirror, preserving the exact
     12-record route pair without changing C authority or the route ledger.
81.) Record Phase 85k's canonical global-state admission. Independent C,
     Swift, and ASan traces plus publication snapshots passed exact parity,
     tamper/rerun, and `fixture_only=0` fences for row
     `0xb123ff3e997bdc78`; the isolated global report SHA-256 is
     `c64d6cff061bcd43df5fa1b5551f8d49d0e80be352d51756a471186cc03f0e2e`.
82.) Record Phase 85l's four-row cumulative merge. The authoritative manifest
     remains 7,420 rows; the cumulative report now has 4 terminal passed rows
     and 7,416 planned, with report SHA-256
     `139cf48c3768d205f82531d47ae4bcbb20cca6ce178eeb0a0384a99e9453e979`.
     M34 pixel/soak/direct-display, M35 Developer ID/notary/clean-machine, and
     human acceptance remain open.
83.) Record Phase 85m's documentation reconciliation across README, SM64
     Modern docs, both goal ledgers, `CHANGES`, and porting memory. Preserve
     the four-row cumulative truth, global and cumulative report hashes, all
     Phase 85j–85l handoffs, and fail-closed remaining route/M34/M35/human
     gates. No source, manifest, route ledger, credential, or release mutation
     is implied.
84.) Record Phase 85n's object-state and script-events route pairs. The
     source-backed independent pairs retain 28 object-state records and 1,272
     script-event records over ticks 2 and 3; the effects audit remains
     blocked because native retains 58 records while Swift currently emits 2.
     No effects admission or ledger mutation is allowed.
85.) Record Phase 85o's isolated admission of the canonical object-state row
     `0x862c3d78b60d657c` and script-events row `0x2b0f6063b5463e9c`, including
     independent C/Swift/ASan parity, tamper/rerun fences, and
     `fixture_only=0` proof. The effects row remains planned.
86.) Record Phase 85p's six-row cumulative merge. The authoritative manifest
     remains 7,420 rows; the cumulative report now has 6 terminal passed rows
     and 7,414 planned, with report SHA-256
     `e406840d88f1d3ff99109c20cc6e75c164a1da8978350d12265260b8be02baf1`.
     M34 pixel/soak/direct-display, M35 Developer ID/notary/clean-machine, and
     human acceptance remain open.
87.) Record Phase 85q's documentation reconciliation across README, SM64
     Modern docs, both goal ledgers, `CHANGES`, and porting memory. Preserve
     the six-row cumulative truth, the effects 58-versus-2 blocker, all
     Phase 85n–85p handoffs, and fail-closed remaining route/M34/M35/human
     gates. No source, manifest, route ledger, credential, or release mutation
     is implied.
88.) Record the Phase 85q collision-query and RNG-draw source-backed pairs:
     204 domain-7 collision records and 168 domain-8 RNG records, each with
     exact independent C/Swift/ASan parity, tamper rejection, and optimized
     evidence. The effects row remains blocked and unadmitted.
89.) Record Phase 85r's isolated admission of collision-query row
     `0xc3294578ae77bee8` and RNG-draw row `0x7632df135b85e448`; preserve the
     independent artifact hashes, report hashes, and rerun/partial/duplicate
     fences without mutating the cumulative ledger.
90.) Record Phase 85s's eight-row cumulative merge. The authoritative manifest
     remains 7,420 rows; the cumulative report now has 8 terminal passed rows
     and 7,412 planned, with report SHA-256
     `91632bff276e0ecc99e845532f6b4d876a80ad237fbb660364a575313cbdc412`.
     Phase 85t reconciles the docs while preserving the effects 58-versus-2
     blocker and the M34/M35/clean-machine/human acceptance gates.
91.) Record the Phase 85u audio-sequence and save-bytes source-backed pairs:
     four domain-9 audio receipts and four domain-10/save receipts with exact
     C/Swift/ASan/optimized parity and matching save sidecars. Keep native PCM,
     device audio, effects, and full persistence authority separate.
92.) Record Phase 85v's isolated admission of the canonical audio-sequence row
     `0xbe184196f54f8216` and save-bytes row `0x4e5552533aaa717d`, including
     independent artifact, tamper, partial, rerun, and `fixture_only=0` fences.
     The effects and `audio_pcm` rows remain planned.
93.) Record Phase 85w's ten-row cumulative merge. The authoritative manifest
     remains 7,420 rows; the cumulative report now has 10 terminal passed rows
     and 7,410 planned, with report SHA-256
     `693e70c316756c369577ccdf263f8b40f6d0b0766f92195b23cb4c979a9ccc35`.
     Phase 85x reconciles the docs while preserving the effects 58-versus-2,
     audio PCM, and M34/M35/human acceptance boundaries.
94.) Record Phase 85z's render-packet and audio-PCM boundary. The independent
     render pair contains 8 domain-11 records and exact C/Swift/ASan/Release
     trace SHA-256 `379fc6cc84990d2ed67223df19dc78d540b3f7ba1768abc2ebad7a61eea01179`;
     the PCM audit reaches two real pre-device callbacks but zero canonical
     domain-9/kind-5 receipts, so `audio_pcm` remains planned.
95.) Record Phase 85aa's isolated render-packet admission and explicit
     `gpu_capture=separate`, `pixel_acceptance=unverified`, and
     `visual_acceptance=unverified` boundaries. No cumulative ledger or GPU
     acceptance mutation is implied.
96.) Record Phase 85ab's eleven-row cumulative merge. The authoritative
     manifest remains 7,420 rows; the cumulative report now has 11 terminal
     passed rows and 7,409 planned, with report SHA-256
     `a67d6b3415a9327ac73c8fc37d9b529c3edce6fe4aae56c9d08c80f72b59d5d3`.
     Phase 85ac reconciles the docs while preserving effects/PCM and
     M34/M35/visual/physical/human acceptance boundaries.
97.) Record Phase 85ac's owner-thread fixed-width PCM receipt seam after
     native synthesis and before device playback. Raw PCM bytes, AVAudio
     pointers, and realtime callback instrumentation remain outside Swift.
98.) Record Phase 85ad's isolated admission of the canonical `audio_pcm` row
     `0x4aa75cc09d180fce`, with two exact domain-9/kind-5 receipts,
     C/Swift/ASan/Release trace equality, receipt equality, and all tamper,
     partial, single-artifact, and rerun fences. Audible/device parity remains
     unverified.
99.) Record Phase 85ae's twelve-row cumulative merge. The authoritative
     manifest remains 7,420 rows; the cumulative report now has 12 terminal
     passed rows and 7,408 planned, with report SHA-256
     `5afa6c4b80f75fa70d18bfc4aab9b499266e69a8fdb61e11bc9a24a18a6cc958`.
     Phase 85af reconciles the docs while preserving the effects, audible/
     device, visual, M34/M35, physical, and human acceptance boundaries.
100.) Record Phase 85af's interaction-state source-backed pair. The observed
      empty-start window retains 14 domain-4/state records over ticks 2 and 3;
      Swift serializes only fixed-width interaction values while C retains
      collision and interaction authority and effects remain unadmitted.
101.) Record Phase 85ag's isolated admission of interaction-state row
      `0x3e1cdaca08b21f54`, report SHA-256
      `2b8916452ceb54cf85fd18defcc883d211a5f1da41f04fd16c665d5dd5040457`,
      with C/Swift/ASan/Release equality, tamper/partial/single-artifact and
      terminal rerun fences. No cumulative ledger mutation is implied.
102.) Record Phase 85ah's thirteen-row cumulative merge. The authoritative
      manifest remains 7,420 rows; the cumulative report now has 13 terminal
      passed rows and 7,407 planned, with report SHA-256
      `ac3e2c19162fdcab8c938497d48398dbac8eb03004d2918a4524292573f9d8ad`.
      Phase 85ai reconciles the docs while preserving the effects, interaction
      authority, audible/device, visual, M34/M35, physical, and human gates.
103.) Record Phase 85ai's effects receipt parity repair. Native publishes the
      fixed-width owner-thread receipt before the schema-4 effect record;
      Swift independently reconstructs all 58 records while raw PCM and
      realtime AVAudio pointers remain outside the seam.
104.) Record Phase 85aj's isolated admission of effects row
      `0x3951f0333dc3c5da`, isolated report SHA-256
      `0c3fa33cd0d219a0e5347d6131397301e48699f91ee17a82c0c029607115a93d`,
      and trace SHA-256
      `68329f0a22e7d20f5ddb3b22777d74e626523d5c0467ccac569566eb6c998226`.
      Device effects, haptic feel, audible output, and human acceptance remain
      unverified.
105.) Record Phase 85ak's fourteen-row cumulative merge. The authoritative
      manifest remains 7,420 rows; the cumulative report now has 14 terminal
      passed rows and 7,406 planned, with report SHA-256
      `4eccd90e978fbcd0cad42f8f64ba96c25774698c49b919ba1e3fdb020b1e0cd2`.
      Phase 85al reconciles the docs while preserving device/haptic/audible,
      visual, M34/M35, physical, and human acceptance boundaries.
106.) Record Phase 85am's level-script, save-mutation, and transition route
      work. Save mutation reaches source-backed global/save receipts and an
      isolated admission; the authored level-script pair emits no transition,
      and the bounded no-floor transition probe remains blocked without a
      source-authored recipe.
107.) Record Phase 85am's save-mutation admission for row
      `0x022fbda0ff7f2dd1`, isolated report SHA-256
      `19dc977d7b8927089f7f22d22e1c35f5ab961ccc0a3c8ba62f741e6f221c29d6`,
      with exact 16-record C/Swift/ASan/Release and sidecar/snapshot equality.
      Level-script and transition rows remain unadmitted.
108.) Record Phase 85ao's fifteen-row cumulative merge. The authoritative
      manifest remains 7,420 rows; the cumulative report now has 15 terminal
      passed rows and 7,405 planned, with report SHA-256
      `dfa2dd3c56fa8e5a97e1f1b699843b40aec24dfbcb8d3094825a8ace2cbd5c7c`.
      Phase 85ap reconciles the docs while preserving level/transition
      blockers and M34/M35/device/audible/visual/physical/human gates.
109.) Record Phase 85aq's disjoint Luna-max route-family and production-gate
      triage. Behavior, display-list, geo/collision, render/audio, and
      level/transition candidates remain planned; M34 is blocked by the locked
      offline host/GPU-tools service and M35 by signing/notary prerequisites.
      No manifest, cumulative ledger, or release artifact changed.
110.) Record Phase 85ar's first source-bound route batch. The pendulum native
      seam retains 64 authored ticks and two clock receipts but cadence-aware
      Swift domains do not yet pair; the `find_floor` call-site is unreached;
      and the selected audio asset has no authored sequence-12/PCM identity.
      No route row was promoted.
111.) Record Phase 85as's authored level-script/transition investigation. The
      CotMC pair remains exact for 505 global/script records with transition=0;
      all initialized movement probes retain floor, and the alternate death
      warp is not the `mario.c:1778` no-floor branch.
112.) Record Phase 85at's M34 production retry. The read-only host gate reports
      no online display and a locked console; `gputoolsserviced`, pixel PNGs,
      long-soak metrics, direct-display interaction, and physical acceptance
      remain unavailable.
113.) Record Phase 85au's M35 readiness retry. Ordinary Xcode 26.6 and the M9/
      M35 contracts pass, but no Developer ID identity/private key or supported
      `notarytool` credentials exist; no archive/export/notarization/stapling,
      clean-machine Gatekeeper, or human acceptance result exists.
114.) Record Phase 85av's canonical audit and docs reconciliation. The
      authoritative manifest remains 7,420 rows; the cumulative report remains
      15 terminal passed and 7,405 planned with report SHA-256
      `dfa2dd3c56fa8e5a97e1f1b699843b40aec24dfbcb8d3094825a8ace2cbd5c7c`.
      All per-phase local commit attempts are retained as blocked while the
      managed checkout rejects `.git/index.lock` writes.
115.) Record Phase 85aw's final conservative audit. Strict Swift 6,
      C/Swift route-pair, sanitizer/Release, and Metal 4 source/archive/scene
      contracts pass; route qualification remains 15/7,420 = 0.202156334%.
      M34 device/pixel/soak, M35 release/Gatekeeper, and fresh-save human
      acceptance remain blocked or not-run, so the implementation and
      acceptance floors stay 0%.
116.) Record Phase 85bb's canonical merge of the display-list and
      render-callback admissions. The authoritative manifest remains 7,420
      rows; the cumulative report now has 18 terminal passed and 7,402 planned,
      with report SHA-256
      `c5c6377b5a2378bd996d91a9d231d0c94070edd623e2b8019f0eeac626ebac74`.
      M34/M35 and human acceptance remain separate external gates.
117.) Record Phase 85bd's final audit. Strict Swift/Metal and acceptance
      contract checks pass or fail closed as expected, but the current camera
      `find_floor` rerun drifts from the retained admission (`actual=11383`
      versus `11391`, coverage `0x9a76a4bb357f867e` versus `0x1c41224c64ab005f`).
      Camera reproducibility repair is required before refreshed promotion.
118.) Record Phase 85be's camera reproducibility repair. Direct pair runs now
      use fresh build roots and restore exact 30-record C/Swift/ASan/Release
      parity for `0x1e3500f9eb2b95d4`, with coverage
      `0x1c41224c64ab005f` and all rerun/negative fences passing.
119.) Record Phase 85bf's fail-closed route-breadth attempts. Native-only RNG
      receipts were retained; text asset generation and RR behavior entry did
      not reach live C/Swift pairing. The canonical state remains 18 terminal /
      7,402 planned.
120.) Record Phase 85bg's exact JRB RNG pair: 40 receipts at tick 2084 with
      C/Swift/ASan/Release byte equality and tamper rejection. Admission fences
      remain outstanding and the canonical row stays planned; water-level
      remains unreachable in the authored JRB recipe.
121.) Record Phase 85bh's corrected RNG admission and canonical merge. The
      instrumentation-only `random_seed_get` row was removed from the generated
      inventory, the manifest remains 7,420 rows, and the report now has 19
      terminal passed / 7,401 planned with SHA-256
      `e21b7c385df0bba86431634b99b04d0e92f188bbe9294912aa4f3f4fc988842c`.
122.) Record Phase 85bj's authored text-save admission and canonical merge.
      The manifest remains 7,420 rows; the report now has 20 terminal passed /
      7,400 planned with SHA-256
      `d9d932987046f489b81110c58668aa5a7359d8bbeeb8d895bea10e135e004b2f`.
123.) Record Phase 85bk's second authored inside-castle display-list pair and
      the fail-closed SL RNG-float reachability probe. The pair is not yet
      admitted; no canonical row changed.
124.) Record Phase 85bl's second display-list admission and current-build
      repair. The canonical report now has 21 terminal passed / 7,399 planned
      with SHA-256
      `071c6bd6e9d15cdf379a2602072ba24fe0d689205baed6744f84f4b142b9c82b`.
125.) Record Phase 85bn's current 21-row audit. Object-state `actual=2965`
      and camera `actual=11391` are aligned; strict route/Metal contracts pass,
      while broad legacy parity and M34/M35/human gates remain open.
126.) Record Phase 85bq's isolated admission of the authored inside-castle
      display-list shard `0x009e431051dba428` (`inside_castle_seg7_dl_07043A68`).
      C/Swift/ASan/Release/rerun traces and packet sidecars are byte-identical;
      the isolated report SHA-256 is
      `6b346ddd8660ce96ae42069bd8cadae06efc1668809762f0e709cb2975700c0a`.
127.) Record Phase 85br's canonical merge of the inside-castle admission. The
      authoritative manifest remains 7,420 rows; the cumulative report now
      has 22 terminal passed and 7,398 planned, with report SHA-256
      `7cbfe09e0b8ecce06908701fb12f66e528d8963a13a2b8b10eebe0795d3639e3`.
      M34/M35 distribution and human acceptance remain separate open gates.
128.) Record Phase 85bs/85bt's authored wooden-door display-list pair and
      isolated admission. `door_seg3_dl_03014EF0` reaches through its authored
      parent `door_seg3_dl_03014F98`; C/Swift/ASan/Release/rerun and all
      negative fences pass. Admission report SHA-256 is
      `2ce9528e63c43da26617ebef3b2260c826a2914838fc0e3bf3aaf915071e6ebb`.
129.) Record Phase 85bu's canonical merge of the door admission. The
      authoritative manifest remains 7,420 rows; the cumulative report now
      has 23 terminal passed and 7,397 planned, with report SHA-256
      `af0068294ae4506f8456a2bb54659745cbc16fb2f4e33466ab4c2a53decff28e`.
      M34/M35 distribution and human acceptance remain separate open gates.
130.) Record Phase 85bw's read-only M34 event-gate audit. `gputoolsserviced`
      is launchd-running, but the GUI remains locked, no display is online,
      no GPU replay session exists, and thermal queries still return
      `0xe00002bc`; no pixel, soak, or physical acceptance claim changed.
131.) Record Phase 85bv's fail-closed authored pendulum behavior triage. Castle
      Inside area 2 reaches native slot 37, but native emits 1,059 records over
      ticks 2–65 versus Swift's 1,056 over ticks 1–64; identity-normalized
      diagnostics still diverge on source-object position. No route promotion
      or canonical ledger mutation is allowed until those contracts align.
132.) Record Phase 85bx's fail-closed HMC elevator behavior candidate. The
      authored area-1 route reaches the area, but native reports
      `hmcPlatformSlot=0`, no object-domain records, and no target identity;
      the standalone Swift/C kernel fingerprint and strict/ASan/optimized
      checks pass without producing an admissible schema-4 route pair.
133.) Record Phase 85by's fail-closed authored intro transition probe. The
      real recipe emits 195 script records over ticks 3–241 with no failures,
      but transition ID 3 remains absent because 120 owner steps cover only 60
      legacy frames before the authored `SLEEP(75)` completes. No synthetic
      transition or canonical route promotion is allowed.
134.) Record Phase 85ca's 150-step authored intro retry. The isolated Debug
      native run emits 240 script records with zero failures/errors, but still
      reaches zero transition-ID-3 records; no Swift pair or canonical route
      promotion is allowed.
135.) Record Phase 85cc's fail-closed render/audio candidate. Native lifecycle
      evidence retains 289,362 schema-4 records, 720 PCM receipts/callbacks,
      and 391,680 frames for sequence asset `0x12`, but zero sequence-12
      records. Swift source-header and negative fences pass; exact C/Swift
      pairing and canonical promotion remain deferred until an authored
      high-score recipe reaches `play_star_fanfare()`.
136.) Record Phase 85cd's authored high-score audio reachability. The
      `SM64_MODERN_AUTOMATED_CASTLE_AREA2=1` route reaches sequence `0x12` at
      tick 63 with 720 PCM receipts and 391,680 playback frames. Exact C/Swift
      per-PCM binding remains deferred, so no canonical route promotion occurs.
137.) Record Phase 85ce's exact source-bound PCM pairing. Sequence `0x12`
      matches across C/Swift/ASan/Release for 720 PCM records/receipts/
      callbacks and 391,680 frames with all negative fences. The full
      476,365-record trace still diverges outside the PCM projection at record
      355, so canonical admission remains deferred.
138.) Record Phase 85cf's full-trace reconciliation. The divergence is now
      localized to record 355, byte offset 45576, in a domain-12/kind-4
      non-PCM payload (`tick=1`, `subject=11`, `record_id=4`, `sequence=10`).
      The 720-record PCM contract remains exact, but non-PCM provenance must be
      resolved before canonical admission.
139.) Record Phase 85cg2's static audio payload provenance. Record 355 offset
      45576 is `OBJECT_SPAWN` `values[1]`, a behavior identity; unknown scripts
      use an anchor-relative pointer delta that varies across build layouts.
      The exact authored subject-11 behavior remains unproven, so whole-trace
      admission stays fail-closed.
140.) Record Phase 85ch's source behavior identity proof. Source ordering proves
      subject 11 is the first Castle Inside area-1 macro sign using authored
      `bhvSignOnWall`; its expected semantic FNV identity is
      `0x58c5c9f354614b2d`. Build-layout pointer identities remain unstable, so
      semantic mapping and exact full-trace parity remain required.
141.) Record Phase 85ci's source-bound `bhvSignOnWall` semantic mapping. Subject
      11 now matches identity `0x58c5c9f354614b2d` across Debug/ASan/Release,
      but whole native traces still diverge at record 361/offset 64 with
      layout-dependent values. Full C/Swift pairing and canonical admission
      remain deferred.
142.) Record Phase 85cj's audio full-trace provenance. Record 361 `values[1]`
      maps to authored `bhvOneCoin` for macro-yellow-coin subjects 17–20, with
      expected semantic identity `0xa4425fa3db847308`; build-layout fallback
      values remain unstable, so full parity and admission are deferred.

143.) Record Phase 85ck's source-bound `bhvOneCoin` semantic mapping
      (`0xa4425fa3db847308`). Syntax compilation passed while fresh full-trace
      parity and negative fences did not complete in the bounded run; retained
      pre-change traces are not current evidence and canonical admission stays
      deferred.
144.) Record Phase 85cl's fresh full audio parity audit. Debug/ASan/Release/
      Rerun retain 476,365 records, sequence 12 at tick 63, and exact PCM
      parity, but cross-build full parity diverges on `bhvFloorTrapInCastle`
      and `bhvCastleFloorTrap` pointer identities. Correct `bhvOneCoin` FNV is
      `0xc4e3fcc926a6842`; prior `0xa442...` evidence is stale.
145.) Record Phase 85cm's floor-trap semantic mapping. `bhvFloorTrapInCastle`
      (`0xccb7e7b3ab117769`) and `bhvCastleFloorTrap`
      (`0x0c688e3d5e7a403e`) stabilize floor-trap records, but ASan later
      diverges at object-despawn identity and Release at `bhvPaintingDeathWarp`;
      full audio admission remains fail-closed.

146.) Record Phase 85cn's despawn/painting identity mapping. Subject 54 is
      authored `bhvBooInCastle` (`0xa63b55d3bf9ba914`) and subject 23 is
      `bhvPaintingDeathWarp` (`0xe53422734eda5e67`), but retained build values
      remain layout-dependent; fresh rerun/fences are required before admission.
147.) Record Phase 85co's mapped audio rerun harness. Strict Swift verifier,
      shell syntax, semantic identity encoding, and truncated-artifact rejection
      pass, but no fresh native rerun completed; canonical promotion remains
      deferred on the latest retained layout-dependent divergence.

148.) Record Phase 85cp's fail-closed native audio rerun. Execution stopped
      during the Debug native-core build before any route probe, so no fresh
      traces, PCM receipts, parity, or negative-fence result exists; the
      canonical 23/7,397 state is unchanged.
149.) Record Phase 85cq's completed fresh Debug audio route. The trace is
      60,974,792 bytes with 476,365 records and ASan/Release/rerun sidecars,
      but Swift projection stops at a `bhvSignOnWall` mapping mismatch; the
      first cross-build divergence is record 1426/domain 3 subject 27. No
      parity or canonical admission claim is allowed.
150.) Record Phase 85cr's source mapping for record 1426. Subject 27 is
      authored `bhvPaintingStarCollectWarp` (`0xc00b59b883354537`) with
      subjects 27–30 bound to Castle painting star-collect warp coordinates;
      Release/rerun/Swift fences did not finish, so parity and admission remain
      deferred.

151.) Record Phase 85cs's fresh subject-27 audio parity. Subjects 27–30 match
      `bhvPaintingStarCollectWarp` and PCM/receipt projections across builds,
      but full parity fails at record 1482/offset 189824 for subject 31 actor
      identity; canonical admission remains deferred.
152.) Record Phase 85ct's source-authored subject-31 identity. Subject 31 is
      `bhvDeathWarp` at `levels/castle_inside/script.c:62` with semantic identity
      `0xa22b7ff16730e047`; focused C/Swift/ASan/Release/rerun and negative
      fences pass, but the next mismatch is record 1496/byte 191617, subject 32
      (`bhvAirborneStarCollectWarp`). Canonical audio admission remains deferred.
153.) Record Phase 85cv's M34 host gate recheck. One display is detected but
      offline, no `gpudebug` session is active, and thermal telemetry fails with
      `0xe00002bc`; replay, pixels, soak, direct-display, and physical acceptance
      remain unclaimed.
154.) Record Phase 85cw's M35 distribution gate recheck. Stable Xcode 26.6 and
      both distribution contracts pass, but no valid signing identity, Developer
      ID certificate/private key, or notary credentials exist; no archive/export,
      notarization, clean-machine Gatekeeper, or human acceptance evidence exists.
155.) Record Phase 85cx's subject-32 source triage. The actor is the authored
      90-degree `bhvAirborneStarCollectWarp` at `levels/castle_inside/script.c:61`
      with expected semantic identity `0xa85510394290b349`; retained values are
      layout-dependent and a fresh full matrix is required before promotion.
156.) Record Phase 85cz's canonical audit. The 7,420-row manifest and 23/7,397
      report remain byte-stable (`23c9d3f1…2b715` / `af006829…ff28e`), all
      duplicate/conflict/fixture-only/terminal-rerun fences pass, and the
      object/camera markers remain `actual=2965` / `actual=11391`.
157.) Record Phase 85cy's airborne-star mapping. The source-bound
      `bhvAirborneStarCollectWarp` identity is `0xa85510394290b349`; exact
      C/Swift/rerun, PCM, receipt, and negative-fence seams pass, but parity
      fails next at record 1524/byte 195201 for unresolved subject 34.
158.) Record Phase 85da's launch-death source triage. Subject 34 is authored
      `bhvLaunchDeathWarp` at `levels/castle_inside/script.c:59` with semantic
      identity `0xbe5dc4c2a1630b6a`; subject 35 is the adjacent launch-star
      warp, and the owner mapping/full parity rerun remain required.
159.) Record Phase 85db's launch-death mapping attempt. The semantic owner
      mapping is present and Debug/ASan traces plus PCM/receipts are complete,
      but the bounded Release build stopped before Release/Swift/rerun/negative
      fences; no parity or canonical promotion is claimed.
160.) Record Phase 85dc's completed launch-death rerun. Corrected
      Debug/ASan/Release/rerun/Swift, PCM/receipt, and negative fences pass for
      subject 34; whole-trace parity advances to record 1538/byte 196992,
      subject 35 `bhvLaunchStarCollectWarp`.
161.) Record Phase 85dd's Release-build diagnosis. A fresh Release native-core
      build exits 0 in 24.6 seconds with a valid 355-member archive; earlier
      stops were bounded-interruption artifacts, not a reproduced build error.
162.) Record Phase 85de's launch-star mapping. Subject 35 matches semantic
      `bhvLaunchStarCollectWarp` (`0x0b9ebb9260f83fe`) across the complete matrix,
      PCM/receipts, and negative fences; parity fails next at record 1566/byte
      200576 for subject 37 and no canonical row moved.
163.) Record Phase 85df's hard-air source triage. Subject 37 is authored
      `bhvHardAirKnockBackWarp` at `levels/castle_inside/script.c:56` with
      semantic identity `0x53f6c1e071460d11`; mapping and full rerun remain
      required.
164.) Record Phase 85dg's hard-air mapping. Subject 37 matches semantic
      `bhvHardAirKnockBackWarp` across the complete matrix, PCM/receipts, and
      negative fences; parity fails next at record 1580/byte 202368 for subject
      38 and no canonical row moved.
165.) Record Phase 85dh's airborne-death source triage. Subject 38 is authored
      `bhvAirborneDeathWarp` at `levels/castle_inside/script.c:55` with semantic
      identity `0x53e013ea7d7cc8b5`; mapping and full rerun remain required.
166.) Record Phase 85di's airborne-death mapping. Subject 38 matches semantic
      `bhvAirborneDeathWarp` across the complete matrix, PCM/receipts, and
      negative fences; parity fails next at record 1594/byte 204160 for subject
      39 and no canonical row moved.
167.) Record Phase 85dj's airborne-warp source triage. Subject 39 is authored
      `bhvAirborneWarp` at `levels/castle_inside/script.c:54` with semantic
      identity `0x3e6af9ed47c59929`; mapping and full rerun remain required.
168.) Record Phase 85dk's airborne-warp mapping. Subject 39 matches semantic
      `bhvAirborneWarp` across the complete matrix, PCM/receipts, and negative
      fences; parity fails next at record 1608/byte 205953 for subject 40 and no
      canonical row moved.
169.) Record Phase 85dl's instant-active source triage. Subject 40 is authored
      `bhvInstantActiveWarp` at `levels/castle_inside/script.c:53` with semantic
      identity `0xf961678fe6b653ea`; mapping and full rerun remain required.
170.) Record Phase 85dm's instant-active mapping. Subjects 40–41 match semantic
      `bhvInstantActiveWarp` across the complete matrix, PCM/receipts, and
      negative fences; parity fails next at record 1636/byte 209536 for subject
      42 and no canonical row moved.
171.) Record Phase 85dn's warp source triage. Subject 42 is authored `bhvWarp`
      at `levels/castle_inside/script.c:49` with semantic identity
      `0x2b006194588201ff`; mapping and full rerun remain required.
172.) Record Phase 85do's warp mapping. Subject 42 matches semantic `bhvWarp`
      (`0x2b006194588201ff`) across the complete matrix, PCM/receipts, and
      negative fences; parity fails next at record 1734/byte 222080 for subject
      49 and no canonical row moved.
173.) Record Phase 85dp's star-door source triage. Subject 49 is the second
      authored eight-star `bhvStarDoor` at `levels/castle_inside/script.c:24`
      with semantic identity `0xda6397948f7ac5cd`; mapping and full rerun remain
      required.
174.) Record Phase 85dq's star-door mapping. Subject 49 matches semantic
      `bhvStarDoor` across the complete matrix, PCM/receipts, and negative
      fences; parity fails next at record 1762/byte 225664 for subject 51 and no
      canonical row moved.
175.) Record Phase 85dr's Toad-message source triage. Subject 51 is authored
      `bhvToadMessage` at `levels/castle_inside/script.c:262` with semantic
      identity `0x00c91057a2eb6ffc`; mapping and full rerun remain required.
176.) Record Phase 85ds's Toad-message mapping. Subject 51 matches semantic
      `bhvToadMessage` across the complete matrix, PCM/receipts, and negative
      fences; parity fails next at record 1804/byte 231040 for subject 55 and no
      canonical row moved.
177.) Record Phase 85dt's Tank-fish source triage. Subject 55 is authored
      `bhvTankFishGroup` at `levels/castle_inside/script.c:258` with semantic
      identity `0x82764ca860723a66`; mapping and full rerun remain required.
178.) Record Phase 85du's Tank-fish mapping. Subject 55 matches semantic
      `bhvTankFishGroup` across the complete matrix, PCM/receipts, and negative
      fences; parity fails next at record 1860/byte 238208 for subject 59 and no
      canonical row moved.
179.) Record Phase 85dv's Fish-group mapping. Subject 59 matches semantic
      `bhvFishGroup` across the complete matrix, PCM/receipts, and negative
      fences; parity fails next at dynamic sparkle-spawner effect record
      3244/byte 415368 and no canonical row moved.
180.) Record Phase 85dw's sparkle-spawner mapping. The dynamic
      `bhvSparkleParticleSpawner` identity is exact across the effect matrix and
      fences; parity fails next at tick 3 record 3347/byte 428552 for `bhvCloud`.
181.) Record Phase 85dx's Cloud mapping. Dynamic `bhvCloud` identity is exact
      across the effect matrix and fences; parity fails next at tick 3 record
      3413/byte 437000 for a `bhvCloudPart` child.
182.) Record Phase 85dy's Cloud-part mapping. Dynamic `bhvCloudPart` identity
      is exact across the effect matrix and fences; parity fails next at tick 3
      record 3865/byte 494848 for `bhvClockMinuteHand`.
183.) Record Phase 85dz's Clock-minute mapping. Dynamic `bhvClockMinuteHand`
      identity is exact across the matrix and fences; parity fails next at tick
      3 record 3879/byte 496640 for `bhvClockHourHand`.
184.) Record Phase 85ea's final Clock-hour route evidence. All five 476,365-
      record full traces are byte-identical (`15273415…198e5d`), PCM/receipts
      and negative fences pass; canonical admission remains the next phase.
185.) Record Phase 85eb's fail-closed audio-route admission. The exact trace
      matrix is not promoted because coverage is deferred (`coverage_fingerprint=0`)
      and the stream contains unrelated domains; a source-backed audio-only
      capture with nonzero coverage is required.
186.) Record Phase 85ef's source-backed audio-only coverage repair. The runtime
      emits 1,084 audio-only records with coverage `0x553ab8ef49275722`; C/Swift/
      ASan/Release/rerun, PCM/receipts, and fences pass; canonical admission is next.
187.) Complete Phase 85f0 isolated canonical admission for audio-asset shard
      `0x03345fc560c65b75`. Independent trace/PCM/receipt artifacts and the
      proof sidecar pass all phase-local negative fences; report SHA is
      `7c22c62f13e25c430069bdfe1c77840fd5f6751737a3c63b3ff6361e88bdb19d`,
      proof SHA is `6dda3168db361324d0283056476be0cef7dc95c249225d757d874303c4bb2081`,
      and the 23/7,397 canonical report remained unchanged at that isolated
      phase pending Phase 85f1.
188.) Complete Phase 85f1's cumulative audio-asset merge. The isolated row
      `0x03345fc560c65b75` joins the 23 prior terminal rows for 24 terminal /
      7,396 planned; merged report SHA is
      `9a68a65a3e20838fab76d35014fd46172e9435de00e8e5b2148d44c0eee4985f`.
      The manifest SHA remains `23c9d3f1aff0a8980c0a7e5104867e123681cc681ebe7e10929284e9cac2b715`.
189.) Record Phase 85f2's bounded pendulum route attempt and M34 re-audit.
      The pendulum remains fail-closed at 1,087 native versus 1,056 Swift
      records with identity/coverage divergence; M34 remains blocked by the
      locked console, offline display, no active GPU session, and thermal
      error `0xe00002bc`.
190.) Record Phase 85g2's M35 distribution re-audit. Stable Xcode 26.6 and
      readiness/distribution contracts pass, but Developer ID identity/private
      key and supported notary authentication are absent; no release,
      Gatekeeper, or human-acceptance result exists.
191.) Record Phase 85f3's pendulum lifecycle repair and four-way matrix.
      Debug/ASan/Release/rerun pairs each contain 1,056 matched records with
      semantic identity `0x6268765f647065` and coverage
      `0x680ff75430bf24ff`; the route remains planned pending admission.
192.) Complete Phase 85f4/85f5 pendulum admission and canonical merge. The
      pendulum row is now cumulative terminal evidence 25/7,395; merged report
      SHA is `aa8eadcb63a555ed3cca7bf2d8c592f2798270633dce4c2d16d0acc79bf87c3d`.
193.) Record Phase 85f6 isolated admission for authored JRB `random_u16` row
      `0x00576356a427dbc2`. Exact 40-record C/Swift/ASan/Release parity and
      negative fences pass; the row was already terminal in the cumulative
      ledger and the isolated report SHA is
      `3055c4bc5f492a0b8779c45129727dbf2e90da76edea1a5e53352209f729d0ea`.
194.) Record Phase 85f6 BBH display-list discovery. Candidate
      `0x000670ec2a57dfa8` is reachable/source-authored but stays planned
      because no pointer-free BBH packet seam exists and nested display-list
      provenance is unresolved.
195.) Record Phase 85f8 RNG-float reachability. The authored Moneybag source
      exists, but the initialized lifecycle remains in area 2 with zero runtime
      Moneybags and zero source-owned route receipts; no Swift pair or
      canonical mutation is permitted.
196.) Record Phase 85f9 water-level reachability. JRB environmental water data
      is present, but no authored Sushi object reaches `find_water_level`; shard
      `0x023fe9bb4409460b` remains planned and no synthetic route is allowed.
197.) Record Phase 85f10 route-breadth audit. No new candidate met authored
      reachability, pointer-free ownership, independent C/Swift parity,
      four-way configuration parity, and admission gates; route state remains
      25 terminal / 7,395 planned.
198.) Record Phase 85f11 DDD Sushi discovery. DDD reaches two authored Sushi
      objects and `find_water_level` statically, but no pointer-free owner/query
      receipt exists; shard `0x023fe9bb4409460b` remains planned.
199.) Record Phase 85f17's documentation reconciliation after the Phase 85f14
      audit. Current status headings now point to Phase 85f11, the historical
      Phase 85aw 15/7,420 counter is restored, `gputoolsserviced` wording is
      corrected, and the f-series handoff indexes are serialized without
      changing source, canonical manifests, or reports.
200.) Record Phase 85f18's authored DDD Sushi source-seam retry. No qualifying
      pointer-free owner/query receipt exists; shard `0x023fe9bb4409460b`
      remains planned and no synthetic route or canonical artifact changed.
201.) Record Phase 85f19's authored BBH display-list seam retry. No
      source-defined pointer-free owner/packet receipt exists; candidate
      `0x000670ec2a57dfa8` remains planned and no route or manifest changed.
202.) Record Phase 85f20's M34 production re-audit. One detected display is
      offline (`online=0`), `gputoolsserviced` is not running, no GPU session
      is active, and thermal state is unknown (`0xe00002bc`); no replay,
      capture, pixel, soak, or direct-display evidence is claimable.
203.) Record Phase 85f21's M35 distribution preflight. Ordinary Xcode 26.6 and
      the readiness/distribution contracts pass, but no Developer ID identity/
      private key or supported `notarytool` authentication exists; no artifact,
      Gatekeeper, or human-acceptance result exists.
204.) Record Phase 85f22's canonical route audit. Fresh deterministic manifest
      regeneration matches 7,420 rows and SHA `23c9d3f1…2b715`; the retained
      report remains 25/7,395 with SHA `aa8eadcb…bf87c3d`. Full replay stopped
      because the transient pendulum artifact expired; no canonical state changed.
205.) Reconcile Phase 85f23 current-status documentation. Preserve the exact
      7,420 manifest, 25/7,395 report, 0.336927224% live-route rate, 95.693%
      behavior mapping, and 0% conservative floors while linking Phases 85f18–
      85f22 in order. No source, manifest, route report, or acceptance claim
      changed.
206.) Record Phase 85f24's authored environment-effect route discovery. Five
      of six planned envfx rows have source-authored level/geo reachability,
      but no pointer-free RNG/floor/water value receipt exists; no route row or
      canonical artifact changed.
207.) Record Phase 85f25's authored intro transition reachability. The real
      155-step owner-thread window reaches transition ID 3, but the existing
      Swift script observer rejects the intro trace as `out_of_order` at its
      tick-2 ordering contract; no independent pair or admission is claimed.
208.) Record Phase 85f26's DDD/JRB camera-water candidates. The generic
      collision receipt lacks camera owner/call-site/mode/query-position
      identity, so neither candidate qualifies and the manifest/report remain
      unchanged.
209.) Reconcile Phase 85f27 current-status documentation. Preserve the exact
      7,420 manifest SHA `23c9d3f1…2b715`, 25/7,395 report SHA
      `aa8eadcb…bf87c3d`, 0.336927224% live-route rate, 95.693% behavior
      mapping, and 0% conservative floors while linking Phases 85f23–85f26 in
      order. No source, manifest, route report, or acceptance claim changed.
210.) Record committed Phase 85f30's source-authored intro transition pair.
      The exact C/Swift/ASan/Release/rerun seam passes, while canonical
      admission remains deferred and no manifest, report, or ledger changes.
211.) Record committed Phase 85f31's source-owned camera-water seam. Runtime
      remains blocked: the ordinary owner-thread recipe does not reach authored
      DDD area 1, so the focused gate fails closed with exit 77 and emits no
      route record or admission; canonical artifacts remain unchanged.
212.) Record committed Phase 85f32's isolated admission of authored intro
      shard `0x9a0f7b4f7ecf6c41`; the isolated report/proof and phase-local
      fences pass without mutating the checked-in manifest, report, or ledger.
213.) Record committed Phase 85f33's guarded phase-local merge. The isolated
      report is 26 terminal / 7,394 planned with SHA
      `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`, or
      `26/7420 = 0.350404313%`; phase-local canonical ID mapping is
      `0x9a0f7b4f7ecf6c41 -> 0xca33981b30cb7815`. The retained checked-in
      canonical state remains 25/7,395 with report SHA `aa8eadcb…bf87c3d` and
      manifest SHA `23c9d3f1…2b715`; the isolated result is not checked-in
      canonical state.
214.) Reconcile Phase 85f34 documentation. Preserve the retained canonical
      7,420-row manifest, 25/7,395 report, exact retained rate
      `25/7420 = 0.336927224%`, isolated rate `26/7420 = 0.350404313%`, and
      0% M34/M35/human-acceptance floors while linking Phases 85f30–85f33 in
      order. No source, manifest, retained report, or acceptance claim changed.
215.) Record committed Phase 85f35's DDD camera reachability probe. The
      ordinary owner-thread lifecycle fails closed with exit 77 and retains
      zero event-307 route records; the C trace and blocked rerun are identical
      72-byte header-only files. The exact unblock is an authored Castle area 3
      DDD-painting traversal into DDD area 1 without direct level/mode/Sushi/
      coordinate/helper shortcuts; canonical artifacts remain unchanged.
216.) Reconcile Phase 85f36 documentation. Preserve the retained canonical
      7,420-row manifest, 25/7,395 report, isolated intro 26/7,394 result, and
      0% M34/M35/human-acceptance floors while linking Phases 85f34–85f35 and
      this refresh in order. No source, manifest, retained report, or
      acceptance claim changed.
217.) Record committed Phase 85f37's Castle-to-DDD traversal discovery. The
      static source-authored chain through Castle Grounds, Castle area 1, Castle
      area 3's DDD painting, and DDD area 1 is present, but no deterministic
      authored owner-thread input recipe crosses it. The bounded probe fails
      closed with exit 77, retains `event-307=0`, and the C trace and blocked
      rerun are identical 72-byte header-only files with SHA
      `a00652d483085b681254ca74c782bbe5e7bdc9d2672ff724ebd3735a3d706512`;
      canonical artifacts remain unchanged.
218.) Reconcile Phase 85f38 final first-party documentation. Preserve the
      retained canonical 7,420-row manifest, 25/7,395 report, isolated intro
      26/7,394 result, and 0% M34/M35/human-acceptance floors while linking
      Phases 85f36–85f37 and this refresh in order. No source, report, route
      ledger, or acceptance claim changed.
219.) Record committed Phase 85f39's serial-publication audit. The isolated
      Phase85f33 report is valid at 26 terminal / 7,394 planned with SHA
      `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`, or
      `26/7420 = 0.350404313%`; target report/proof SHA values are
      `6563b63155e1b5f4465b30dd51c5c60a02bfe08459f8e6885549ed716ceef7d2` and
      `c6de62ac55499a20d92ac5d7ec67cb328d569df276219bfde53f95b85900fc51`.
      The retained canonical state remains 25/7,395 at
      `25/7420 = 0.336927224%`; explicit authorization is required before
      canonical route-ledger publication, and no canonical artifact changed.
220.) Record committed Phase 85f40's Castle-to-DDD recipe blocker. The static
      source chain is present, but no deterministic authored owner-thread input
      recipe crosses the basement door into Castle area 3 and DDD area 1; the
      bounded probe fails closed with exit 77, retains `event-307=0`, and
      produces no route admission. The known C trace remains the identical
      72-byte header-only artifact with SHA
      `a00652d483085b681254ca74c782bbe5e7bdc9d2672ff724ebd3735a3d706512`.
221.) Record committed Phase 85f41's fresh M34 re-audit. The host gate remains
      `m34_host_ready=0` with one offline display, a locked console/session,
      stopped `gputoolsserviced`, no active GPU session, absent fresh traces,
      and unknown thermal state (`0xe00002bc`); replay, pixels, cadence, soak,
      direct-display, physical visual/feel, and human-acceptance evidence are
      not claimable, so the M34 floor remains 0%.
222.) Record committed Phase 85f42's fresh M35 re-audit. Ordinary Xcode 26.6
      and the readiness/distribution contracts pass, but zero valid Developer
      ID identities and no notary authentication leave distribution blocked;
      no archive/export/DMG/ZIP/notarization/stapling/Gatekeeper or
      human-acceptance artifact exists, so M35 and human-acceptance floors
      remain 0%.
223.) Reconcile Phase 85f43 first-party documentation. Preserve the exact
      retained manifest/report hashes, canonical 25/7,395 and isolated 26/7,394
      percentages, the explicit publication-authorization boundary, the
      Castle-to-DDD traversal blocker, fresh M34/M35 blockers, and 0% M34/M35/
      human-acceptance floors while linking Phases 85f39–85f42 and this refresh
      in order. No source, manifest, report, route ledger, or acceptance claim
      changed.
224.) Record Phase 85f44's isolated-admission inventory. The retained report
      remains exactly 25 terminal / 7,395 planned; one unrepresented,
      source-authored, `fixture_only=0`, hash-valid candidate is the Phase
      85f33 intro report, guarded from local ID `0x9a0f7b4f7ecf6c41` to
      canonical ID `0xca33981b30cb7815`. Its phase-local report remains
      26/7,394, no other isolated evidence is admissible, and no canonical
      artifact changed.
225.) Record Phase 85f45's authored WDW express-elevator discovery. Dynamic
      `bhvWdwExpressElevator` candidate `0x6e6c6a0fc1b92a45` has an authored
      area-1 lifecycle and an existing Swift value/owner route; static sibling
      `0xd52a32f6de0311da` is excluded. A source-bound C observer/semantic
      identity and live C/Swift pair remain required; no admission or merge
      occurred.
226.) Record Phase 85f46's bounded Metal 4 contract re-audit. Source,
      archive/presentation, capture-archive guard, M9 readiness, timebase, and
      shell contracts pass, but the visible-host gate remains
      `m34_host_ready=0` with an offline display, locked session, no GPU
      session, stopped GPU tooling, and unknown thermal state. Runtime capture,
      replay, pixel, cadence, soak, and direct-display evidence remain
      inadmissible; M34 remains 0%.
227.) Reconcile Phase 85f48 first-party documentation. Preserve exact retained
      25/7,395 and isolated 26/7,394 facts, the 0% M34/M35/human-acceptance
      floors, and ordered Phase 85f44–85f46 links. Phase 85f47 is explicitly
      pending because no handoff/result is present; no f47 outcome, counter, or
      acceptance claim is asserted. No source, manifest, report, route ledger,
      or acceptance state changed.
228.) Record committed Phase 85f47's WDW express-elevator receipt seam. The
      source-owned dynamic observer, pointer-free schema-4 packet, independent
      Swift mirror, and focused C/Swift contract are implemented, but the
      authored Castle-to-WDW lifecycle reaches `level=11 (LEVEL_WDW), area=2`
      before a dynamic receipt is emitted. The matrix fails closed with
      `dynamic=0`, `static=0`, and exit 77; no route record, admission,
      manifest, retained report, or canonical ledger mutation occurred, and
      the static sibling was not substituted. Runtime remains blocked at WDW
      area 2.
229.) Reconcile Phase 85f50 first-party documentation. Preserve exact retained
      25/7,395 and isolated 26/7,394 facts, all hashes and percentages, and the
      0% M34/M35/human-acceptance floors while linking the f47 seam, prior f48
      refresh, committed f49 handoff, and this refresh in order. No source,
      manifest, report, route ledger, or acceptance state changed.
230.) Reconcile Phase 85f51 first-party documentation after Phase 85f49's WDW
      reachability audit. The authored Castle-to-WDW painting route
      targets warp node `0x0A` but lands in WDW area 2 before the dynamic
      elevator can tick; the matrix fails closed with `dynamic=0`, `static=0`,
      and exit 77, with no trace, route record, or admission. Preserve the
      retained 25/7,395 and isolated 26/7,394 facts and the 0% M34/M35/
      human-acceptance/full-goal floors while linking f47–f51 in order.
231.) Reconcile Phase 85f54 documentation after the f50/f51 corrections and
      Phase 85f52 TTC 2D rotator discovery. Record f49's actual WDW area-2
      reachability blocker (`dynamic=0`, `static=0`, `admission=0`,
      `canonical_ledger_mutation=0`, `manifest_mutation=0`, exit 77,
      `trace=not-created`), preserve the exact canonical 25/7,395 and isolated
      26/7,394 counters/hashes/percentages and 0% M34/M35/human floors, retain
      authored `bhvTTC2DRotator` row `0x1af5669b06931d93` as planned, and note
      that no f53 completion handoff was present at that refresh. No source,
      manifest, report, route ledger, or acceptance state changed.
232.) Record Phase 85f53's committed TTC 2D rotator seam and correct the
      superseded status wording. The authored Castle Inside painting route lands in
      `level=14 (LEVEL_TTC), area=2` with `hands=0`; the matrix fails closed
      with exit 77 before a trace or admission, and no route record, manifest,
      retained report, or canonical ledger mutation occurred. Preserve the
      exact retained 25/7,395 and isolated 26/7,394 counters, hashes,
      percentages, and 0% M34/M35/human-acceptance/full-goal floors while
      linking the ordered handoffs in `f53 -> f54 -> f55` sequence:
      `porting-handoff-full-swift-twin-phase85f53-ttc-rotator-seam-execute.md`,
      `porting-handoff-full-swift-twin-phase85f54-docs-refresh.md`, and
      `porting-handoff-full-swift-twin-phase85f55-docs-ttc-correction.md`. No
      source, manifest, report, route ledger, or acceptance state changed.
233.) Reconcile Phase 85f57 documentation after the committed Phase 85f56
      TTC area-1 reachability audit. The bounded owner-thread probe ends at
      `level=14 (LEVEL_TTC), area=2, hands=0` and exits 77; its direct gate
      bypasses authored Castle Inside painting nodes `0x21`–`0x23`, so it does
      not establish a source-authored TTC area-1 lifecycle. No C trace was
      opened or Swift trace written (`trace=not-created`, `records=0`), and
      no C/Swift pairing, admission, route record, manifest, retained report,
      or canonical ledger mutation occurred; TTC row
      `0x1af5669b06931d93` remains planned. Preserve retained canonical
      25/7,395 with manifest SHA-256
      `23c9d3f1aff0a8980c0a7e5104867e123681cc681ebe7e10929284e9cac2b715`,
      report SHA-256
      `aa8eadcb63a555ed3cca7bf2d8c592f2798270633dce4c2d16d0acc79bf87c3d`,
      `25/7420 = 0.336927224%`, isolated 26/7,394 with SHA-256
      `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`,
      `26/7420 = 0.350404313%`, and 0% M34/M35/human-acceptance/full-goal
      floors while linking the ordered `f56 -> f57` handoffs:
      `porting-handoff-full-swift-twin-phase85f56-ttc-area1-reachability.md`
      and `porting-handoff-full-swift-twin-phase85f57-docs-ttc-blocker-refresh.md`.
      No source, manifest, report, route ledger, or acceptance state changed.
234.) Reconcile Phase 85f63 documentation after the f59 publication-wrapper
      audit, f60 acceptance/timebase audit, f61 drift attribution, and f62 Bob
      seesaw seam. The current merge tool expects 25 generated targets and 104
      tokens while its wrapper still supplies 23 report/proof pairs and 50
      tokens, so no serial publication was authorized or performed. M34/M35
      remain blocked; the timebase fixture still records 715 object-timer and
      289 inventory matches versus current 718 and 290, while actual RNG-call
      syntax remains 289 and the fixture update is unauthorized. Bob runtime
      fails closed at `level=1 area=1 object=0` with exit 77 and no admission.
      Preserve retained canonical 25/7,395 with manifest SHA-256
      `23c9d3f1aff0a8980c0a7e5104867e123681cc681ebe7e10929284e9cac2b715`,
      report SHA-256
      `aa8eadcb63a555ed3cca7bf2d8c592f2798270633dce4c2d16d0acc79bf87c3d`,
      `25/7420 = 0.336927224%`, isolated intro 26/7,394 with SHA-256
      `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`,
      `26/7420 = 0.350404313%`, and 0% M34/M35/human-acceptance/full-goal
      floors while linking the ordered `f58 -> f63` handoffs:
      `porting-handoff-full-swift-twin-phase85f58-next-route-discovery.md`,
      `porting-handoff-full-swift-twin-phase85f59-publication-wrapper-audit.md`,
      `porting-handoff-full-swift-twin-phase85f60-acceptance-state-audit.md`,
      `porting-handoff-full-swift-twin-phase85f61-timebase-drift-diagnosis.md`,
      `porting-handoff-full-swift-twin-phase85f62-seesaw-seam-execute.md`, and
      `porting-handoff-full-swift-twin-phase85f63-docs-refresh.md`. No source,
      manifest, report, route ledger, or acceptance state changed.
235.) Reconcile Phase 85f66 documentation after the f64 serial-merge dry-run
      and f65 timebase-fixture proposal. The non-destructive serial dry-run
      fails closed because four retained Phase 85f4 pendulum proof artifacts
      expired; no first-stage/final output or publication was produced. The
      retained timebase audit still fails on exactly two intentional deltas:
      `object_timer` 715 -> 718 and `random_calls` 289 -> 290 inventory
      matches; actual RNG-call syntax remains 289 and the fixture update was
      not applied. Preserve retained canonical 25/7,395 with manifest SHA-256
      `23c9d3f1aff0a8980c0a7e5104867e123681cc681ebe7e10929284e9cac2b715`,
      report SHA-256
      `aa8eadcb63a555ed3cca7bf2d8c592f2798270633dce4c2d16d0acc79bf87c3d`,
      `25/7420 = 0.336927224%`, isolated intro 26/7,394 with SHA-256
      `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`,
      `26/7420 = 0.350404313%`, and 0% M34/M35/human-acceptance/full-goal
      floors while linking the ordered `f63 -> f64 -> f65 -> f66` handoffs:
      `porting-handoff-full-swift-twin-phase85f63-docs-refresh.md`,
      `porting-handoff-full-swift-twin-phase85f64-serial-merge-dryrun.md`,
      `porting-handoff-full-swift-twin-phase85f65-timebase-fixture-proposal.md`,
      and `porting-handoff-full-swift-twin-phase85f66-docs-refresh.md`. No
      source, manifest, report, fixture, route ledger, publication, or
      acceptance state changed.
236.) Reconcile Phase 85f70 documentation after the fresh Phase 85f67 pendulum
      evidence, Phase 85f68 immutability fix, and green Phase 85f69 two-stage
      serial dry-run. The fresh four-way pendulum matrix and isolated admission
      pass with 1,056 records/matches per run, semantic identity
      `0x6268765f647065`, coverage `0x680ff75430bf24ff`, and unchanged
      canonical manifest; f68 now hashes every retained proof-artifact URL,
      including trace/log paths, before and after dry-run. F69 passes first
      stage 25/7,395 with report SHA
      `aa8eadcb63a555ed3cca7bf2d8c592f2798270633dce4c2d16d0acc79bf87c3d` and
      final phase-local stage 26/7,394 with SHA
      `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`, or
      `26/7420 = 0.350404313%`; all negative fences and retained-artifact
      mutation guards pass. Preserve retained canonical 25/7,395 at
      `25/7420 = 0.336927224%`, manifest SHA
      `23c9d3f1aff0a8980c0a7e5104867e123681cc681ebe7e10929284e9cac2b715`,
      behavior mapping 95.693%, and 0% M34/M35/human-acceptance/full-goal
      floors. Link the ordered `f63 -> f70` handoffs:
      `porting-handoff-full-swift-twin-phase85f63-docs-refresh.md`,
      `porting-handoff-full-swift-twin-phase85f64-serial-merge-dryrun.md`,
      `porting-handoff-full-swift-twin-phase85f65-timebase-fixture-proposal.md`,
      `porting-handoff-full-swift-twin-phase85f66-docs-refresh.md`,
      `porting-handoff-full-swift-twin-phase85f67-pendulum-evidence-refresh.md`,
      `porting-handoff-full-swift-twin-phase85f68-dryrun-immutability-fix.md`,
      `porting-handoff-full-swift-twin-phase85f69-serial-dryrun-fence-fix.md`,
      and `porting-handoff-full-swift-twin-phase85f70-docs-refresh.md`. No
      source, manifest, report, fixture, route ledger, publication, or
      acceptance state changed.
237.) Reconcile Phase 85f74 documentation after the Phase 85f71 Spindrift
      discovery and Phase 85f72 serial-publication readiness audit. The
      source-authored `bhvSpindrift` row
      `0x028a122a6b0f0fa2` remains planned; the f72 isolated serial result
      preserves retained 25/7,395 with report SHA-256
      `aa8eadcb63a555ed3cca7bf2d8c592f2798270633dce4c2d16d0acc79bf87c3d`
      and phase-local 26/7,394 with SHA-256
      `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`,
      while manifest SHA-256 remains
      `23c9d3f1aff0a8980c0a7e5104867e123681cc681ebe7e10929284e9cac2b715`.
      Preserve `25/7420 = 0.336927224%`, `26/7420 = 0.350404313%`, behavior
      mapping 95.693%, and 0% M34/M35/human-acceptance/full-goal floors.
      Phase 85f73 then committed the Spindrift receipt seam (`eb3fbf8a`):
      the authored route reached `level=1 area=1 spindrifts=0`, exited 77,
      and produced no trace or admission. Link the ordered f71 -> f74
      handoffs:
      `porting-handoff-full-swift-twin-phase85f71-next-route-discovery.md`,
      `porting-handoff-full-swift-twin-phase85f72-serial-publication-readiness.md`,
      `porting-handoff-full-swift-twin-phase85f73-spindrift-seam-execute.md`,
      and `porting-handoff-full-swift-twin-phase85f74-docs-refresh.md`. No
      source, manifest, report, route ledger, publication, or acceptance state
      changed.

238.) Correct the stale Phase 85f73 status wording across the six first-party
      status surfaces. Record the committed Spindrift seam (`eb3fbf8a`), the
      authored `level=1 area=1 spindrifts=0` result, exit 77, and
      `trace=not-created`, `records=0`, `admission=0`. Preserve retained
      25/7,395 with report SHA-256
      `aa8eadcb63a555ed3cca7bf2d8c592f2798270633dce4c2d16d0acc79bf87c3d`,
      isolated 26/7,394 with SHA-256
      `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`,
      manifest SHA-256
      `23c9d3f1aff0a8980c0a7e5104867e123681cc681ebe7e10929284e9cac2b715`,
      percentages `25/7420 = 0.336927224%` and `26/7420 = 0.350404313%`,
      behavior mapping 95.693%, and 0% M34/M35/human-acceptance/full-goal
      floors. Link the ordered f71 -> f75 handoffs, including the existing
      f74 handoff and the new
      `porting-handoff-full-swift-twin-phase85f75-docs-spindrift-correction.md`.

239.) Reconcile Phase 85f79 documentation after the Phase 85f76 Spindel
      discovery and Phase 85f77 read-only publication-action audit. Preserve
      the authored SSL area-2 `bhvSpindel` row
      `0xdc93743116807bec` as planned with Swift owner contract
      `0x2f02c221a0c4104e`; the f78 source receipt seam is now committed, but
      no native lifecycle pair or admission exists. The f77 f72/f69 serial
      outputs are byte-identical, but
      no write-once designation or canonical publication was performed.
      Preserve retained 25/7,395 with report SHA-256
      `aa8eadcb63a555ed3cca7bf2d8c592f2798270633dce4c2d16d0acc79bf87c3d`,
      isolated 26/7,394 with SHA-256
      `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`,
      manifest SHA-256
      `23c9d3f1aff0a8980c0a7e5104867e123681cc681ebe7e10929284e9cac2b715`,
      percentages `25/7420 = 0.336927224%` and
      `26/7420 = 0.350404313%`, behavior mapping 95.693%, and 0% M34/M35/
      human-acceptance/full-goal floors. Phase 85f78 committed the Spindel seam
      (`91e53c7f`); the authored Castle→SSL route remained at
      `level=1 area=1 spindels=0`, exited 77, and produced no trace or
      admission. Link the ordered f76 -> f79 sequence:
      `porting-handoff-full-swift-twin-phase85f76-next-route-discovery.md`,
      `porting-handoff-full-swift-twin-phase85f77-publication-action-audit.md`,
      `porting-handoff-full-swift-twin-phase85f78-spindel-seam-execute.md`, and
      `porting-handoff-full-swift-twin-phase85f79-docs-refresh.md`. No source,
      manifest, report, route ledger, publication, or acceptance state changed.

240.) Correct the stale Phase 85f78 status wording across the six first-party
      status surfaces and the existing Phase 85f79 handoff. Record the
      committed Spindel seam (`91e53c7f`), the authored Castle→SSL route
      result `level=1 area=1 spindels=0`, exit 77, and
      `trace=not-created`, `records=0`, `admission=0`. Preserve retained
      25/7,395 with report SHA-256
      `aa8eadcb63a555ed3cca7bf2d8c592f2798270633dce4c2d16d0acc79bf87c3d`,
      isolated 26/7,394 with SHA-256
      `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`,
      manifest SHA-256
      `23c9d3f1aff0a8980c0a7e5104867e123681cc681ebe7e10929284e9cac2b715`,
      percentages `25/7420 = 0.336927224%` and
      `26/7420 = 0.350404313%`, behavior mapping 95.693%, and 0% M34/M35/
      human-acceptance/full-goal floors. Link the ordered f76 -> f80
      handoffs:
      `porting-handoff-full-swift-twin-phase85f76-next-route-discovery.md`,
      `porting-handoff-full-swift-twin-phase85f77-publication-action-audit.md`,
      `porting-handoff-full-swift-twin-phase85f78-spindel-seam-execute.md`,
      `porting-handoff-full-swift-twin-phase85f79-docs-refresh.md`, and
      `porting-handoff-full-swift-twin-phase85f80-docs-spindel-correction.md`.
      No source, manifest, report, route ledger, publication, or acceptance
      state changed.
241.) Record the Phase 85f81 committed write-once designation and Phase 85f82
      first-party status transition. The designated local canonical report is
      `build/sm64-modern-phase85f81-serial-publication/run.elhzBC/canonical-route-ledger.tsv`
      with 7,420 rows, 26 terminal `passed`, 7,394 `planned`, and SHA-256
      `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`.
      Preserve the old retained report as the byte-identical write-once backup
      with SHA-256
      `aa8eadcb63a555ed3cca7bf2d8c592f2798270633dce4c2d16d0acc79bf87c3d`
      and the unchanged manifest SHA-256
      `23c9d3f1aff0a8980c0a7e5104867e123681cc681ebe7e10929284e9cac2b715`.
      Keep the designated local evidence separate from the historical backup;
      do not claim push, release, store publication, or human acceptance.
      Preserve M34, M35, human-acceptance, and full-goal floors at 0%, link
      `porting-handoff-full-swift-twin-phase85f81-serial-canonical-designation.md`
      and `porting-handoff-full-swift-twin-phase85f82-docs-canonical-transition.md`,
      and leave source, manifest, and old retained report unchanged.

242.) Reconcile Phase 85f83–85f87 across the six first-party status surfaces.
      Record the fresh authored-reachability rechecks: WDW
      `level=11 area=2 dynamic=0 static=0`, TTC `level=14 area=2 hands=0`,
      Bob `level=1 area=1 object=0`, and Spindrift
      `level=1 area=1 spindrifts=0`; each exits 77 with no C/Swift trace,
      route record, or admission. Preserve the designated local 26/7,394
      report SHA-256
      `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`
      and `26/7420 = 0.350404313%` separately from the byte-identical
      25/7,395 backup SHA-256
      `aa8eadcb63a555ed3cca7bf2d8c592f2798270633dce4c2d16d0acc79bf87c3d`
      and `25/7420 = 0.336927224%`; retain manifest SHA-256
      `23c9d3f1aff0a8980c0a7e5104867e123681cc681ebe7e10929284e9cac2b715`,
      behavior mapping 95.693%, and 0% M34/M35/human-acceptance/
      implementation/full-goal floors. Link the ordered f81 -> f87 handoffs,
      including `porting-handoff-full-swift-twin-phase85f83-wdw-reachability-recheck.md`,
      `porting-handoff-full-swift-twin-phase85f84-ttc-reachability-recheck.md`,
      `porting-handoff-full-swift-twin-phase85f85-bob-reachability-recheck.md`,
      `porting-handoff-full-swift-twin-phase85f86-spindrift-reachability-recheck.md`,
      and `porting-handoff-full-swift-twin-phase85f87-docs-refresh.md`.
      No source, manifest, report, route ledger, release, publication, or
      acceptance state changed.

243.) Reconcile Phase 85f88 across the six first-party status surfaces.
      Record source-route discovery for the authored Snowman's Land area-1
      `bhvSLSnowmanWind` row `0xa98dae7d4d4559ab`, whose direct `MODEL_NONE`
      subject is `script_func_local_3` source order 1 at `(700,3428,700)`,
      face yaw 30, parameter 0, reached through the ordinary Castle Inside
      painting route. The pointer-free Swift value/owner pair is mapped once,
      but no source observer, native lifecycle probe, C/Swift trace, route
      record, or admission was produced; the row remains planned. Preserve
      the designated local 26/7,394 report SHA-256
      `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`
      and `26/7420 = 0.350404313%` separately from the byte-identical
      25/7,395 backup SHA-256
      `aa8eadcb63a555ed3cca7bf2d8c592f2798270633dce4c2d16d0acc79bf87c3d`
      and `25/7420 = 0.336927224%`; retain manifest SHA-256
      `23c9d3f1aff0a8980c0a7e5104867e123681cc681ebe7e10929284e9cac2b715`,
      behavior mapping 95.693%, and 0% M34/M35/human-acceptance/
      implementation/full-goal floors. Link the Phase 85f88 discovery
      handoff `porting-handoff-full-swift-twin-phase85f88-next-route-discovery.md`;
      Phase 85f89 remains pending because no completed handoff exists. No
      source, manifest, report, route ledger, release, publication, or
      acceptance state changed.

244.) Correct the stale Phase 85f89-pending wording across the six first-party
      status surfaces and the existing Phase 85f90 handoff. Record the
      committed Snowman wind receipt seam (`8e2ab88b`): the authored Castle
      Inside Snowman's Land route remains at `level=1 area=1 wind=0`, exits
      77, and creates no trace or admission (`trace=not-created`, `records=0`,
      `admission=0`). Preserve the designated local 26/7,394 report SHA-256
      `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`
      and `26/7420 = 0.350404313%` separately from the byte-identical
      25/7,395 backup SHA-256
      `aa8eadcb63a555ed3cca7bf2d8c592f2798270633dce4c2d16d0acc79bf87c3d`
      and `25/7420 = 0.336927224%`; retain manifest SHA-256
      `23c9d3f1aff0a8980c0a7e5104867e123681cc681ebe7e10929284e9cac2b715`,
      behavior mapping 95.693%, and 0% M34/M35/human-acceptance/
      implementation/full-goal floors. Link the ordered f88 -> f91 handoffs:
      `porting-handoff-full-swift-twin-phase85f88-next-route-discovery.md`,
      `porting-handoff-full-swift-twin-phase85f89-snowman-wind-seam-execute.md`,
      `porting-handoff-full-swift-twin-phase85f90-docs-refresh.md`, and
      `porting-handoff-full-swift-twin-phase85f91-docs-snowman-wind-correction.md`.
      No source, manifest, report, route ledger, publication, or acceptance
      state changed by this documentation correction.

245.) Reconcile Phase 85f92 across the six first-party status surfaces.
      Record source-route discovery for Jolly Roger Bay's authored
      `bhvTreasureChestsJrb` root at route row `0x246e8a98cbad9a7a`. Its
      area-1 source script creates the four-step puzzle through the source
      root and four source-owned children; the existing pointer-free Swift
      Treasure Chest owner preserves the JRB variant, child order, scalar
      puzzle state, interaction results, and effect intents. This is gameplay
      puzzle evidence, not a generic-water query; discovery leaves the row
      planned with no native load/helper call, synthetic child/object
      injection, trace, source edit, manifest/report/ledger mutation, or
      admission. Preserve the designated local 26/7,394 report SHA-256
      `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`
      and `26/7420 = 0.350404313%` separately from the byte-identical
      25/7,395 backup SHA-256
      `aa8eadcb63a555ed3cca7bf2d8c592f2798270633dce4c2d16d0acc79bf87c3d`
      and `25/7420 = 0.336927224%`; retain manifest SHA-256
      `23c9d3f1aff0a8980c0a7e5104867e123681cc681ebe7e10929284e9cac2b715`,
      behavior mapping 95.693%, and 0% M34/M35/human-acceptance/
      implementation/full-goal floors. Link the ordered f88 -> f92 handoffs,
      including `porting-handoff-full-swift-twin-phase85f89-snowman-wind-seam-execute.md`,
      `porting-handoff-full-swift-twin-phase85f90-docs-refresh.md`,
      `porting-handoff-full-swift-twin-phase85f91-docs-snowman-wind-correction.md`,
      and `porting-handoff-full-swift-twin-phase85f92-next-route-discovery.md`;
      Phase 85f93 remains pending because no completed handoff exists. No
      source, manifest, report, route ledger, release, publication, or
      acceptance state changed.

246.) Correct the stale Phase 85f93-pending wording across the six first-party
      status surfaces and the existing Phase 85f94 handoff. Record the
      committed JRB treasure-chest receipt seam (`904bffa3`): the authored
      Castle→JRB route remains at
      `level=1 area=1 roots=0 bottoms=0 tops=0`, exits 77 before creating a
      trace or admitting a route (`trace=not-created`, `records=0`,
      `admission=0`), and the selected row remains planned. The seam preserves
      semantic root/bottom/top identities, source child ordinals 1 through 4,
      authored scalar state, interaction outcomes, and effect intents without
      exposing native pointers. Preserve the designated local 26/7,394 report
      SHA-256
      `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`
      and `26/7420 = 0.350404313%` separately from the byte-identical
      25/7,395 backup SHA-256
      `aa8eadcb63a555ed3cca7bf2d8c592f2798270633dce4c2d16d0acc79bf87c3d`
      and `25/7420 = 0.336927224%`; retain manifest SHA-256
      `23c9d3f1aff0a8980c0a7e5104867e123681cc681ebe7e10929284e9cac2b715`,
      behavior mapping 95.693%, and 0% M34/M35/human-acceptance/
      implementation/full-goal floors. Link the ordered f88 -> f95 handoffs:
      `porting-handoff-full-swift-twin-phase85f88-next-route-discovery.md`,
      `porting-handoff-full-swift-twin-phase85f89-snowman-wind-seam-execute.md`,
      `porting-handoff-full-swift-twin-phase85f90-docs-refresh.md`,
      `porting-handoff-full-swift-twin-phase85f91-docs-snowman-wind-correction.md`,
      `porting-handoff-full-swift-twin-phase85f92-next-route-discovery.md`,
      `porting-handoff-full-swift-twin-phase85f93-treasure-chest-seam-execute.md`,
      `porting-handoff-full-swift-twin-phase85f94-docs-refresh.md`, and
      `porting-handoff-full-swift-twin-phase85f95-docs-treasure-correction.md`.
      No source, manifest, report, route ledger, release, publication, or
      acceptance state changed by this documentation correction.

247.) Reconcile Phase 85f96 across the six first-party status surfaces and
      record the Whomp's Fortress source-route discovery. The selected
      authored `bhvWhompKingBoss` route row is `0x28e0617bfc286cbe`; its first
      WF area-1 `ACT_1` subject is source ordinal 0, `MODEL_WHOMP` at
      `(0,3584,0)` with behavior parameter `0`, reached through Castle Inside
      painting nodes `0x06`, `0x07`, and `0x08`. The existing value-only
      `WhompEnemy`/`WhompObjectBridge` route preserves King state and the
      semantic reward child. Discovery remains static/source evidence only:
      no native WF load, helper call, synthetic spawn, object injection,
      trace, source edit, manifest/report/ledger mutation, or admission
      occurred; the row remains planned and Phase 85f97 remains pending
      because no completed handoff exists. Preserve the designated local
      26/7,394 report SHA-256
      `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`
      and `26/7420 = 0.350404313%` separately from the byte-identical
      25/7,395 backup SHA-256
      `aa8eadcb63a555ed3cca7bf2d8c592f2798270633dce4c2d16d0acc79bf87c3d`
      and `25/7420 = 0.336927224%`; retain manifest SHA-256
      `23c9d3f1aff0a8980c0a7e5104867e123681cc681ebe7e10929284e9cac2b715`,
      behavior mapping 95.693%, and 0% M34/M35/human-acceptance/
      implementation/full-goal floors. Link the ordered f88 -> f98 handoffs:
      `porting-handoff-full-swift-twin-phase85f88-next-route-discovery.md`,
      `porting-handoff-full-swift-twin-phase85f89-snowman-wind-seam-execute.md`,
      `porting-handoff-full-swift-twin-phase85f90-docs-refresh.md`,
      `porting-handoff-full-swift-twin-phase85f91-docs-snowman-wind-correction.md`,
      `porting-handoff-full-swift-twin-phase85f92-next-route-discovery.md`,
      `porting-handoff-full-swift-twin-phase85f93-treasure-chest-seam-execute.md`,
      `porting-handoff-full-swift-twin-phase85f94-docs-refresh.md`,
      `porting-handoff-full-swift-twin-phase85f95-docs-treasure-correction.md`,
      `porting-handoff-full-swift-twin-phase85f96-next-route-discovery.md`,
      Phase 85f97 pending, and
      `porting-handoff-full-swift-twin-phase85f98-docs-refresh.md`. No source,
      manifest, report, route ledger, release, publication, or acceptance
      state changed.

248.) Correct the stale Phase 85f97-pending wording across the six first-party
      status surfaces and the existing Phase 85f98 handoff. Record the
      committed Whomp King receipt seam (`66ca1911`): the authored Castle→WF
      route remains at `level=1 area=1 whomps=0`, exits 77 before creating a
      trace or admitting a route (`trace=not-created`, `records=0`,
      `admission=0`), and the selected row remains planned. Preserve the
      designated local 26/7,394 report SHA-256
      `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`
      and `26/7420 = 0.350404313%` separately from the byte-identical
      25/7,395 backup SHA-256
      `aa8eadcb63a555ed3cca7bf2d8c592f2798270633dce4c2d16d0acc79bf87c3d`
      and `25/7420 = 0.336927224%`; retain manifest SHA-256
      `23c9d3f1aff0a8980c0a7e5104867e123681cc681ebe7e10929284e9cac2b715`,
      behavior mapping 95.693%, and 0% M34/M35/human-acceptance/
      implementation/full-goal floors. Link the ordered f88 -> f99 handoffs:
      `porting-handoff-full-swift-twin-phase85f88-next-route-discovery.md`,
      `porting-handoff-full-swift-twin-phase85f89-snowman-wind-seam-execute.md`,
      `porting-handoff-full-swift-twin-phase85f90-docs-refresh.md`,
      `porting-handoff-full-swift-twin-phase85f91-docs-snowman-wind-correction.md`,
      `porting-handoff-full-swift-twin-phase85f92-next-route-discovery.md`,
      `porting-handoff-full-swift-twin-phase85f93-treasure-chest-seam-execute.md`,
      `porting-handoff-full-swift-twin-phase85f94-docs-refresh.md`,
      `porting-handoff-full-swift-twin-phase85f95-docs-treasure-correction.md`,
      `porting-handoff-full-swift-twin-phase85f96-next-route-discovery.md`,
      `porting-handoff-full-swift-twin-phase85f97-whomp-seam-execute.md`,
      `porting-handoff-full-swift-twin-phase85f98-docs-refresh.md`, and
      `porting-handoff-full-swift-twin-phase85f99-docs-whomp-correction.md`.
      No source, manifest, report, route ledger, release, publication, or
      acceptance state changed by this documentation correction.

249.) Reconcile Phase 85f102 documentation after the Phase 85f100 THI
      Fire Piranha Plant discovery and Phase 85f101 static source-contract
      seam. Preserve commits `d746b151` and `7541ed67`, exact static-only
      verdict **STATIC SEAM COMPLETE / RUNTIME RECEIPT ABSENT / NO ADMISSION**,
      and contract fingerprint `0xab99743d61a6e50a`; the authored row
      `0x783b75ac5fc8435f` remains planned because no runtime receipt or
      admission exists. Preserve the designated local 26/7,394 report with
      SHA-256 `4982e0157b94cc1ca940983f9121d89b216c0375d413cccdf30257af7a37adc4`
      (`26/7420 = 0.350404313%`) separately from the byte-identical 25/7,395
      backup with SHA-256
      `aa8eadcb63a555ed3cca7bf2d8c592f2798270633dce4c2d16d0acc79bf87c3d`
      (`25/7420 = 0.336927224%`); retain manifest SHA-256
      `23c9d3f1aff0a8980c0a7e5104867e123681cc681ebe7e10929284e9cac2b715`,
      behavior-manifest SHA-256
      `83ed2a4dd580e462fa33f88f3fe126ae726f4a1f4139114f0de5d7d695355ccb`,
      and 0% M34/M35/human-acceptance/implementation/full-goal floors. Link
      the ordered f100 -> f102 handoffs:
      `porting-handoff-full-swift-twin-phase85f100-next-route-discovery.md`,
      `porting-handoff-full-swift-twin-phase85f101-fire-piranha-seam-execute.md`,
      and `porting-handoff-full-swift-twin-phase85f102-docs-refresh.md`. No
      source, manifest, report, route ledger, release, publication, or
      acceptance state changed.

250.) Reconcile Phase 85f105 documentation after the Phase 85f103 Rainbow
      Ride Donut Platform discovery and Phase 85f104 static seam. Preserve
      commit `ea1e4772`, exact static-only verdict **STATIC SEAM COMPLETE /
      RUNTIME RECEIPT ABSENT / NO ADMISSION**, and fingerprint
      `0x8470fe2a9a74008b`; the authored row `0x0114376397887ece` remains
      planned because no runtime receipt or admission exists. Preserve the
      designated 26/7,394 report and byte-identical 25/7,395 backup with
      their existing hashes, the unchanged route/behavior manifest hashes,
      and 0% M34/M35/human-acceptance/implementation/full-goal floors. Link
      the ordered f103 -> f105 handoffs:
      `porting-handoff-full-swift-twin-phase85f103-next-route-discovery.md`,
      `porting-handoff-full-swift-twin-phase85f104-donut-platform-seam-execute.md`,
      and `porting-handoff-full-swift-twin-phase85f105-docs-refresh.md`. No
      source, manifest, report, route ledger, release, publication, or
      acceptance state changed.

Historical pre-Phase-60 continuation entries (items 1–42) retain their
original Phase 1–55 evidence and pre-Phase-55 7,419 denominator wording; they
are not current counters.

1.) Centrally promote the remaining Phase 2/3 owners through dispatch routes
    259–270, including platform, puzzle, Snowman, and Treasure Chest families,
    preserving generation-safe ownership and effect delivery.
2.) Update the deterministic behavior manifest to 534 rows, 511 Swift owners,
    and 23 explicit C adapters at `0x5e5d8c00a7fab8a3`.
3.) Add a bounded live route-shard executor that requires record-mode live
    traces, rejects fixture/missing/reused evidence, and emits isolated worker
    results for canonical batches up to 256 rows.
4.) Close the first real `oracle_hook|input` shard (`0xd9446dfed10e189e`)
    through C/Swift replay and the live worker-result gate, leaving 7,418
    rows explicitly planned.
5.) Preserve the M34c warm-pipeline and resize/pause stress evidence boundary:
    source/build gates pass, but host/compositor throttling still stops the
    visible display-link run at three frames and archive reuse is unproven.
6.) Record the remaining central route promotions, 7,419-shard live closure,
    M35 distribution, and human-acceptance gates in the goal and handoff docs.
7.) Add a fail-closed M35 archive/export/notarize/staple/DMG/ZIP flow that
    performs no mutation while the local Developer ID/notary prerequisites are
    unavailable.
8.) Add the Phase 20 independent schema-4 C/Swift pairing audit. The
    lifecycle/live-route captures remain rejected, while a bounded one-record
    common-input probe matches byte-for-byte and rejects C/Swift tampering;
    no additional route shard is admitted.
9.) Record the Phase 21 M34 closure audit. Metal 4 source and GPU structure
    remain healthy, but host/compositor throttling, archive reuse, clear-only
    attachments, visual/reference, physical-device, and human gates remain
    open.
10.) Record the Phase 22 M35 acceptance audit and Luna-max continuation plan.
     Stable Xcode 26.6 is invocation-scoped and fail-closed; Developer ID,
     Release entitlement, notary, clean-machine, and human acceptance gates
     remain external and unclaimed.
11.) Record Phase 25 route alignment for `oracle_hook|input`. The retained
     1/7,419 live-qualified counter is unchanged; the new one-record attempt
     reports `admitted=0`, `coverage_deferred`, and an incomplete route window.
12.) Record the Phase 26 M34 locked-host capture attempt. API/shader validation
     and capture-only passes remain separate; the host yields three callbacks,
     `archive_reuse=false`, and clear-only black fetched attachments, so no
     visual/device closure is claimed.
13.) Record the Phase 27 Release entitlement correction: Release now has
     `get-task-allow=false` with sustained execution preserved, while Debug
     remains independently debuggable.
14.) Record the Phase 28 stable-Xcode M35 recheck. Release entitlement is no
     longer a blocker; exactly the Developer ID Application identity/private
     key and notary authentication blockers remain, with no artifact mutation.
15.) Reconcile README, SM64 Modern status, and the current Full Swift Twin
     continuation summary without changing historical ledger entries; preserve
     the 534/511/23 and 1/7,419 counters and keep release/visual/full-game
     acceptance explicitly open.
16.) Record Phase 31's complete independent two-tick, byte-identical C/Swift
     window and promotion for the existing `oracle_hook|input` row; the retained
     live-qualified ledger remains 1/7,419 and no second row is admitted.
17.) Record Phase 32's fresh M34 host/compositor refresh: the host remains
     locked/headless, the runtime reaches three frames, archive reuse is false,
     and fetched color/depth attachments are clear-only black.
18.) Record Phase 33's stable-Xcode M35 prerequisite refresh: exactly the
     missing Developer ID Application identity/private key and notarytool
     authentication blockers remain, with the distribution flow fail-closed.
19.) Record Phase 34's next-route qualification boundary: neither
     `bhvDecorativePendulum` nor `oracle_hook|global_state` can be admitted
     without real missing schema domains or Swift emitters/owners.
20.) Reconcile the public/status documents and current continuation summary
     after Phases 31–34 without changing historical ledger text; preserve the
     no-shipped, no-visual-parity, and no-full-game-Swift claims.
21.) Record Phase 37's decorative-pendulum seam audit. No second route is
     admitted: the real Swift pair still lacks collision-query and
     behavior-script/lifecycle owners, while global-state/random-seed ownership
     remains absent for the next oracle row.
22.) Reconcile README, SM64 Modern status, and the current Full Swift Twin
     continuation summary after Phase 37 without changing historical ledger
     text; preserve the 534/511/23 and 1/7,419 counters and the no-shipped,
     no-visual-parity, and no-full-game-Swift claims.
23.) Add a fresh Phase 38 completion audit that keeps the existing
     `oracle_hook|input` row as the only complete route, retains the locked/
     headless M34 boundary and two M35 blockers, and leaves release, device,
     visual, full-game, and human acceptance open.
24.) Record Phase 39's global-state trace audit: the canonical
     `oracle_hook|global_state` row still lacks a schema-4 Swift emitter for
     the native global timer/lifecycle values and shared random-seed owner.
25.) Record Phase 40's decorative-pendulum seam audit: no source-backed
     owner-thread collision world/floor query, per-object behavior-VM/script-
     PC lifecycle plumbing, or shared schema-4 trace sink exists, so no second
     route is admitted.
26.) Record Phase 41's external-state refresh: M34 remains blocked by the
     locked/asleep host, while M35 remains blocked by the same two prerequisites
     (Developer ID Application identity/private key and notarytool auth), with
     no artifact or human-acceptance mutation.
27.) Reconcile README, SM64 Modern status, and the current Full Swift Twin
     continuation summary after Phases 39–41 without changing historical ledger
     text; preserve the 534/511/23 and 1/7,419 counters and the no-shipped,
     no-visual-parity, and no-full-game-Swift claims.
28.) Add the Phase 42 completion audit, retaining the one qualified
     `oracle_hook|input` row, the locked/asleep M34 boundary, the two M35
     blockers, and the exact source/external unblocks for future work.
29.) Record Phase 43's global-state owner boundary: the native timer,
     level/area/act/course lifecycle, and shared random seed still lack one
     source-backed Swift owner-thread schema-4 emitter.
30.) Record Phase 44's real `bhvDecorativePendulum` owner seam, including the
     explicitly bound collision world, decoded behavior program, real floor
     query, lifecycle/script/effect/object-state records, and generation-safe
     ownership.
31.) Record Phase 45's central dispatch configuration for the pendulum owner;
     configured dispatch is source-backed, while the unconfigured identity-only
     route remains trace-silent.
32.) Record Phase 46's source-backed Swift Castle Inside area-2 recipe from the
     real behavior, level-script, collision, and room streams; no C pair or
     route promotion was produced.
33.) Record Phase 47's native-C blocker: monolithic `data/behavior_data.c`,
     `gCurrentObject`, and the legacy level-surface/audio loader prevent a safe
     pendulum schema-4 C owner without a narrow native adapter.
34.) Reconcile the public/status docs and add a Phase 48 completion audit;
     preserve the 534/511/23 and 1/7,419 counters, unchanged global-state/
     Metal/M35 boundaries, and the no-shipped, no-visual-parity, and no-full-
     game-Swift claims.
35.) Record Phase 49's native-core Castle Inside area-2 boundary: the strict
     native archive and existing lifecycle/oracle smoke pass, but the public
     lifecycle API has no owner-thread level/area selection entrypoint or
     exposed `levelCommandAddr`; no pendulum route or second shard is admitted.
36.) Reconcile the public/status docs and add a Phase 50 completion audit after
     Phase 49 without changing historical ledger text; preserve the 534/511/23
     and 1/7,419 counters, the Phase 44–48 owner/dispatch/source/native-C
     boundaries, all M34/M35 boundaries, and the no-shipped, no-visual-parity,
     and no-full-game-Swift claims.
37.) Record Phase 51's compiled Castle Inside level-script result: all three
     area definitions load, including the real area-2 pendulum spawn, but the
     owner-thread command pointer initializes area 1 and enters `CALL_LOOP`
     without a normal area-2 transition operation. Direct `load_area(2)` and
     fabricated globals remain invalid route evidence; no C/Swift pair or route
     promotion is admitted.
38.) Reconcile the public/status docs and add a Phase 52 completion audit after
     Phase 51 without changing historical ledger text; preserve the 534/511/23
     and 1/7,419 counters, Phase 44–50 owner/route/M34/M35 evidence, and the
     no-shipped, no-visual-parity, and no-full-game-Swift claims.
39.) Record Phase 52's real opt-in native Castle Inside area-2 transition:
     the compiled `LEVEL_CASTLE` script reaches area 2 through the normal warp
     path, observes the actual decorative-pendulum pool slot 37, and emits
     native area-2 records; no direct `load_area(2)` or fabricated globals are
     accepted as route evidence.
40.) Record Phase 53's exact Castle area-2 pendulum pairing boundary: the
     filtered native slot-37 trace retains only domains 6/7 and is missing the
     object-state (3) and pendulum-effect (12) domains emitted by the Swift
     recipe; the required run/content/timebase/configuration/initial-save
     fingerprints also diverge, so promotion remains unattempted.
41.) Reconcile the public/status docs and add a Phase 54 completion audit after
     Phases 52–53 without changing historical ledger text; preserve the
     534/511/23 and 1/7,419 counters, unchanged M34/M35 and human-acceptance
     boundaries, and the no-shipped, no-visual-parity, and no-full-game-Swift
     claims.
42.) Correct the Phase 54 route denominator drift: the regenerated source
     inventory and deterministic shard manifest now contain 7,420 rows after
     excluding the Phase 52 `initiate_warp` header declaration while retaining
     legitimate `.c` transition call sites. The retained live row remains
     1/7,420 with 7,419 planned; this correction does not mutate the ledger or
     admit the blocked pendulum route.

Refresh 11
1.) (HEAD -> master, origin/master, origin/HEAD) Make geo_process_level_of_detail endian-independent (#1049)
2.) Label oMoveFlags and slight cleanup. (#1046)
3.) Avoid UB in synthesis_resample_and_mix_reverb (#1048)
4.) Change some void * to correct type (#1047)
5.) Remove oPathedWaypointsS16 and convert all paths to Trajectory struct. (#1045)
6.) Mr Blizzard documentation, Tox Box Unks (#1042)
7.) Pipe input to textconv. (#1041)
8.) Remove erroneous long double casts from audiofile.cpp (#1039)
9.) Replace fixed dialogID in play_dialog_sound to DIALOG_COUNT (#1040)

Refresh #10.1
1.) Diff update (#1033)
2.) Fix texture dimensions for exclamation boxes (#1034)
3.) Fix armips compilation on Windows by changing order of inclusion files (#1035)
4.) Embed libaudiofile into the repo as a single file (#1036)
5.) Fix some tools issues found while compiling on MSYS2 (#1037)

Refresh #10
1.) GCC 9 noreturn UB fixes (#961)
2.) List supported binutils variants in README.md (#960)
3.) fix fly guy's possible values for target roll
4.) clarify that 1 is an exclusive bound in random_float (#962)
5.) replace unkB8 with prevNumStarsForDialog (#963)
6.) Replace "BulletBillUnkF8" with "BulletBillInitialMoveYaw" (#966)
7.) Renamed more warp behaviors and spawn types (#949)
8.) EU bss cleanups (#965)
9.) Add -fno-zero-initialized-in-bss to GCC CFLAGS (#968)
10.) Use empty .SECONDARY target instead of .PRECIOUS (#964)
11.) Match EU seq_channel_layer_process_script (#969)
12.) Fix version variable overwriting bug for non-matching compilations (#974)
13.) small bhv_collect_star_init labeling (#972)
14.) Align SI DMA command buffers, add more names to osCont code (#973)
15.) Minor for loop fix in playback.c (#976)
16.) Add Docker config (Public #36)
17.) Fix usage output crash for skyconv (#977)
18.) Cleanup tools and their build flags (#978)
19.) Print more detailed file reading error messages in extract_assets (#982)
20.) Remove asmdiff (#983)
21.) Fixed tabledesign makefile bug (#985)
22.) Improve first-diff.py (#987)
23.) Bring in some enhancements such as widescreen enhancements (#981)
24.) Fix background shadow border if widescreen in shade_screen (#988)
25.) Merge common EU sections in osExceptionPreamble (#990)
26.) Introduce the NO_SEGMENTED_MEMORY define (#989)
27.) NO_SEGMENTED_MEMORY comments (#992)
28.) Introduce f3dex2e and a GBI_FLOATS setting (#993)
29.) Audio uses signed 16-bit integers as samples, not unsigned (#995)
30.) Assemble RSP ucode with armips (#970)
31.) Label Mr. Blizzard, Yoshi, Whomp, Wiggler, Wood Piece oFields. (#996)
32.) update crash enhancement (#1003)
33.) Update README.md to use new public Discord server invite
34.) Label variables in act_idle. (#1005)
35.) Fix a typo in camera.h (#1009)
36.) Fixed up profiler.c (#1010)
37.) Add -d to first-diff.py (#1012)
38.) Fix typo in first-diff.py (#1013)
39.) Remove binutils 2.27 dependency check (#1015)
40.) Update armips to v0.11.0 tag and add generator script (#1004)
41.) Extract IPL3 font from ROM (#1011)
42.) Documented audio playback flags (#1021)
43.) Change RSP register notation to dollar-prefixed numbers (#1022)
44.) Only check for QEMU_IRIX in Makefile when compiling with IDO (#1023)
45.) Clean up header files and update function prototypes (#1000)
46.) Named local variables in geo_update_layer_transparency (#1019)
47.) Extract crash screen font textures from EU and SH ROMs (#1025)
48.) Use ACTIVE_FLAG defines where appropriate (#1026)
49.) Changed type of actor collision from s16 to Collision (#1024)
50.) Implement sqrtf in assembly file (#1028)
51.) Rename Struct8032FCE8 as OpenableGrill and identify data members (#1029)
52.) Fix some mistakes in the abi doc (#1031)
53.) The README.md PR (#979)

Refresh #8
1.) Use INT_STATUS_ macros for oInteractStatus (#911)
2.) Update bullet_bill.inc.c (#912)
3.) Make geo.c in levels nonconst to reduce diffs with gcc enhancement patch in future (#913)
4.) Improve process_level_music_dynamics (#915)
5.) add AVOID_UB define for fixing libultra aliasing. (#916)
6.) const hack to TARGET_N64 and tweak ld script (#918)
7.) Replace raw level/course numbers with defines (#919)
8.) Label all graph node flags. (#920)
9.) readme.md capitalization
10.) The Big Function PR (Part 5) (#910)
11.) Label hardware registers (#922)
12.) Match EU synthesis_process_note (#923)
13.) Some quick value changes (#909)
14.) Added sequence player defines (#926)
15.) Shindou diffs 2 [merge game.c and display.c, required for SH] (#927)
16.) Match synthesis_process_notes and use -sopt for synthesis.c (#928)
17.) Fix mips64-binutils for Fedora 31 and similar Linux distros (#931)
18.) Update first-diff.py to work with Shindou (#933)
19.) Labelled behavior_script.c (#929)
20.) shindou diffs up to level_update (#932)
21.) Update diff.py and diff_settings.py to work with SH (#936)
22.) Add some Shindou resources to extract_assets.py (#939)
23.) Label and document gd_math.c (#930)
24.) Label all of Mario's actions. (#941)
25.) Label all particles. (#940)
26.) Renamed death related warpBhvs (#942)
27.) Small Shindou differences (#945)
28.) match player_performed_grab_escape_action in all versions (#943)
29.) add main alignment (aglab2, #947)
30.) Match a bunch of fake EU matchings (#944)
31.) Shindou diffs round 4 (#937)
32.) Easy PAL fake matchings (#946)
33.) Merge object_helpers and object_helpers2 headers (#948)
34.) match cap_check_quicksand (#950)
35.) Shindou crash screen diffs (#938)
36.) Thread6 (#951)
37.) Correct IPL3 function and label addresses (#952)
38.) Add COMPILER flag for Makefile and add official mips gcc support. (#953)
39.) add align to sequence_00 (fixes gcc build) (#957)
40.) Fixed itty bitty typo (#959)

Refresh #7
1.) update README.md (#861)
2.) [eu] fix decompilation bugs in synthesis_process_note (#862)
3.) Label and document Snufit.inc.c (#863)
4.) Label and document Snufit.inc.c (#863)
5.) Match append_bubble_vertex_buffer, append_snowflake_vertex_buffer (#860)
6.) Compile audio/effects.c with -sopt (#870)
7.) Match handle_dialog_text_and_pages (#872)
8.) Remove improperly used LEVEL_SA define. (#875)
9.) Remove unused EU ASM files (#873)
10.) Shindou build flag support (#876)
11.) The Big Function PR (Part 1) (#864)
12.) "Re-document" file_select and star_select for EU (#877)
13.) Document paintings.c (#868)
14.) Minor interaction.c labelling/documentation. (#882)
15.) Match synthesis_execute, prepare_reverb_ring_buffer, func_eu_802e00d8 (#886)
16.) The Big Function PR (Part 2) (#874)
17.) Additional painting documentation (#883)
18.) Some EU audio labelling (#885)
19.) Bring synthesis_do_one_audio_update a bit closer to matching (#887)
20.) Misleading comment, this actually contols Bowser's init action (#889)
21.) Match synthesis_do_one_audio_update (#890)
22.) Remove unneeded if(1) in synthesis_do_one_audio_update (#891)
23.) Match synthesis_resample_and_mix_reverb (#893)
24.) Set LANG=C (#896)
25.) Match note_apply_headset_pan_effects (#897)
26.) Match play_sequence (#898)
27.) Match sequence_channel_enable (#899)
28.) Match sequence_player_process_sequence (#900)
29.) Match (eu) alloc_bank_or_seq (#901)
30.) Match alloc_bank_or_seq (#903)
31.) Update README.md (again)
32.) Documented behaviour/fish.inc.c & blue_fish.inc.c (#894)
33.) Labelled 4 behavior files, made object_helper functions consistent (#881)
34.) Add a make target for .copt (#902)
35.) The Big Function PR (Part 3) (#888)
36.) The Big Function PR (Part 4) (#906)
37.) Rename src/audio/memory.c -> heap.c (#907)
38.) Fix BBH texture (#908)

Refresh #6
1.) Make tools work on MinGW (#804)
2.) Document mario_misc (#628)
3.) add patch_libmalloc.py script to work around compiler crashes (#811)
4.) Label the coffins file. (#829)
5.) Convert enhancements into patches (#827)
6.) Document Tweester.inc.c (#840)
7.) EU OK and cleanup EU (#782)

Refresh #5
1.) mem_error_screen.inc.c updated for C (#775)
2.) updated patch_libultra_math to work directly on libultra.a (#781)
3.) Enhancement: Debug boxes (#783)
4.) Label effects MemoryPool (#784)
5.) Use full-width characters for JP text and rename mislabeled stuff (#772)
6.) Small asset extraction fixes (#785)
7.) Fix incremental asset extraction after the last PR (#790)
8.) Labelled behavior script commands, tidied up behavior scripts and ren…
9.) Document camera.c (#741)
10.) Fix a HMC texture asset (#795)
11.) Fix UB in angle conversion for paintings. Needed for pc-port. (#796)
12.) Fix PSS texture assets (#801)
13.) Fix some camera typos (#802)

Refresh #4
1.) Label screen_transition.c (#737)
2.) Revamp macros (#738)
3.) Simplify the cake makefile logic (#739)
4.) Move zbuffer to C (#742)
5.) Audio blob endian/64-bit fixes (#740)
6.) Ub fixes (#745)
7.) process_level_music_dynamics: slightly better stack and regalloc (#746)
8.) move level order specific definitions out to levels/level_defines.h (#743)
9.) Renamed some mislabelled sound effects (#748)
10.) Very minor white space fixes in Mario.c (#752)
11.) Renamed GRAVITY to OBJ_PHYSICS (#755)
12.) Use defined macros instead of literal values for interactions in behavior_data.c (#754)
13.) iQue Player console detection + full support (#756)
14.) Fix Goddard Mario head for little endian etc. (#757)
15.) Small bits of audio doc (#749)
16.) Update diff script (#750)
17.) Nicer format for course/dialog text, enum for dialog IDs (#753)
18.) first-diff: store most recent mtime during check (#759)
19.) Use Lights1 struct for light values (#760)
20.) Detect mips64-elf in diff.py (#761)
21.) Name display lists in gd renderer.c (#764)
22.) Document a variety of PU type crashes. (#765)
23.) Label object_behaviors. (#716)
24.) Update swoop.inc.c (#767)
25.) Label tilting_pyramid.inc.c (#768)
26.) Label red_coin.inc.c (#770)
27.) Use more level defines (#758)
28.) Named Mario actions 6, 7, and 8 and noted causes of hands-free holding glitch (#769)

Refresh #3
1.) Small bits of sound sample labelling
2.) Append 'b' to fopen calls in n64graphics to satisfy Windows
3.) Document synthesis.c and abi.h
4.) Remove gbi_old.h and use gbi.h instead for F3D_OLD
5.) Fix and change most values used by f3d commands
6.) Convert actors to C
7.) Make extract_assets.py fail on bad baserom
8.) Fix some (pragma-ignored) warnings
9.) Convert trig tables and data/ buffers into C
10.) Document intro behaviors and some of camera.c
11.) Match dma_sample_data
12.) Convert levels, bin, text, goddard_tex_dl to C
13.) Remove .bss size hardcoding
14.) Add mips64 fallback and alt dump option to diff.sh
15.) Convert behavior_data to C
16.) Get rid of the enum in behavior_data.
17.) Change more values used by f3d commands
18.) Document game/skybox.c
19.) Actors levels fixup
20.) Convert Mario animations to C (kind of)
22.) generate skybox data from 248x248 images
23.) 64-bit fixes
24.) Move demo data to C
25.) Change uintptr_t to GeoLayout for actors
26.) Move libc includes to include/libc/

Refresh #2a (hotfix)
Changes from Refresh #2:

1.) Fixed text Makefile dependencies"

Refresh #2
Changes from Refresh #1:

1.) Label src/game/behaviors/boo.inc.c
2.) Verify half of audio_defines.h
3.) Label ingame_menu.c and some dialog stuff
4.) Improve handle_dialog_text_and_pages EU non-matching
5.) Fix texture format for water splash/sparkle
6.) Merge ucode.bin and ucode_unk.bin
7.) 64-bit fixes
8.) [AUDIO] Match func_80316928
9.) Documented bug in Tuxie's mother's code
10.) [AUDIO] Slightly closer matching for process_level_music_dynamics
11.) Name channel scripts in sequence 0
12.) Nicer error message when sha1sum doesn't match
13.) Refactor object fields
14.) Document file_select.c
15.) Document star_select.c
16.) Add uncompressed string support to textconv
17.) Document main_menu geo and leveldata
18.) Document hud.c and rename char textures
19.) Correct gSPTextureRectangle values
20.) Linker script wildcard
     - Fixes "undefined reference to x in discarded section y" linker errors.
     - Also means that you no longer have to add linker script entries for source files.
21.) Reference qemu-irix package and add project structure
     - Installing qemu-irix is as simple as installing the provided Debian package on the GitHub "Releases" tab
       (https://github.com/n64decomp/qemu-irix).
     - Old qemu-irix installs are unaffected by this change, and it is still possible to install qemu-irix with the older method.

Refresh #1
Changes from initial release:

1.) More README cleanups and documentation
2.) Fix .gitignore in tools/
3.) More README cleanups and corrections
4.) oUnk190 -> oInteractionSubtype
5.) Normalize line endings
6.) Replace bank_sets.s by sequences.json + build magic
7.) Fix tabledesign in .gitignore
8.) Cleaned and documented arrays in audio/
9.) Change minimum Python 3 requirement in README.md to 3.6
10.) Remove whitespace in ingame_menu.c, save_file.c, and star_select.c
11.) Documentation comment style fixed
12.) sm64.map -> sm64.version.map (ex: sm64.us.map)
13.) Decompile EU versions of HUD and obj behaviors
14.) Label ukiki.inc.c and ukiki_cage.inc.c
15.) Add missing OS header includes
16.) Fix alignment issue in model_ids.h
17.) A bunch of behavior changes:
     a.) Made styles consistent with function names (ex: BehBehaviorLoop -> beh_behavior_loop)
     b.) Split obj_behaviors_2.c into multiple .inc.c files in behaviors/
     c.) bhvHauntedRoomCheck -> bhvCoffinManager
18.) Update README.md notes on WSL and add links
19.) Added tidy.sh and also bracing around single-line ifs and loops
